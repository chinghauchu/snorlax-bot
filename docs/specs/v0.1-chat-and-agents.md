# Snorlax-Bot v0.1 spec — chat layout + agent messaging

Product: Snorlax-Bot
Repo: https://github.com/chinghauchu/snorlax-bot
Audience: Backend, Frontend (macOS Tauri), iOS
Status: **locked 2026-08-25**. Engineering may build from this. CEO confirmed the four defaults. Backend's hop 0→1 / one-mention cut is **rejected**.

**v0.1.1 (CEO 2026-08-25, after Chinghau's test):** 1:1 transcripts are isolated. Agent–agent traffic displays in the seeded `Snorlax-Bot` group channel, not in 1:1s. This overrides the v0.1 "DM-in-1:1s / involve bubble" surface. Hop caps and runtime-routed mentions are unchanged. See §2, §4, and §7.

**v0.2 (CEO 2026-08-25):** Collaboration fluency. A user `@chip` in A's 1:1, or A DMing B, opens a **channel thread** under a LEFT `kind=handoff` card authored as A. Pack B wakes with `{ originating, userAsk, brief, mentionedIds }` (not a quote). A's 1:1 stays user + A + a jump chip when the user caused the handoff. Isolation is unchanged. See §8.

**v0.3 (CEO 2026-08-25):** Agent identity + info pane. Header click (desktop also Cmd+Shift+I) opens a 320px overlay / iOS sheet. `kind=agent` is editable identity; `kind=channel` is a read-only member list. Seed DELETE is 204 with no auto-reseed. Isolation is unchanged. See §9.

**v0.4 (CEO 2026-08-26, after Chinghau's 1:1 @mention screenshot):** Collaboration report-back + extra user-created channels. 1:1 @involves log on seed `snorlax-bot-group` when present. B does the task in the channel thread (no ACK classifier). A reports each result in A's 1:1 as that peer lands (not a hop). Isolation is unchanged: B does not appear as B in A's 1:1. Users can create additional `kind=channel` rows and PATCH their members. See §10.

**CEO override 2026-08-26:** seed channel `snorlax-bot-group` DELETE is **204**, gone from GET, **no auto-reseed** — never recreate `snorlax-bot-group`. Empty roster is OK (no agents and/or no channels) and keeps chrome. After seed channel delete, if anything remains in the roster, select it — **agent first**, else a remaining channel. Sidebar shows Delete on the Snorlax-Bot **row** (right-click / iOS swipe), not in the info pane. No jump chip if no channel remains. New agents auto-join `snorlax-bot-group` only if that row still exists; if the seed is gone they join nothing. Seed channel PATCH stays 409. 1:1 @mention report-back still works with zero channels (B does the ask, not written to B’s 1:1; A answers in A’s 1:1 as A, non-hop, `senderId=A`, no `from` field). A2A log: if `snorlax-bot-group` exists, always log there (ignore body `channelId`). Else if the 1:1 POST body has `channelId` and it is an existing `kind=channel` row, log there. Else skip (no `handoff` field, no jump chip; still run B and A’s report-back). Unknown/stale `channelId` after seed delete: skip the log, do not 422, do not recreate seed.

**v0.4 (CEO 2026-08-26, after Chinghau's 1:1 @mention screenshot):** Collaboration report-back + extra user-created channels. 1:1 @involves log on seed `snorlax-bot-group` only. B does the task in the channel thread (no ACK classifier). A reports each result in A's 1:1 as that peer lands (not a hop). Isolation is unchanged: B does not appear as B in A's 1:1. Users can create additional `kind=channel` rows and PATCH their members. See §10.

**v0.5 (CEO 2026-08-26):** Grok-Bot-like basic tools. Runtime-owned function-calling loop (files, shell, web) in per-agent and per-channel sandboxes under `~/.snorlax-bot` (not a Mac folder picker). Shell has no extra network; HTTP is `web_search` / `web_fetch` only. Tools auto-run (no approval/question widgets). Search provider is env/config, not a hardcoded vendor. No MCP, no computer GUI / live preview. See §11.

**v0.6 (CEO 2026-08-26):** Thin Computer pane over the **existing** `~/.snorlax-bot` workspace. Desktop: 320px **right pane** next to chat (same width language as the identity overlay, but this is a column, not a 320px overlay). Collapsible; default open. iOS: no computer pane this slice (chat + muted tool traces only). Same roots as runtime `workspace_for()`. Clients never call tools or the model; new GETs are runtime reads of the sandbox. No terminal GUI, no screenshot stream, no take-over vs watch-only, no MCP, no question widgets, no Mac folder picker. See §12.

**v0.7 (CEO 2026-08-26):** Runtime MCP client. FastAPI is the MCP client (stdio subprocess + LAN HTTP/SSE). Desktop/iOS never speak MCP. Config is `mcp.json` under `SNORLAX_DATA_DIR` (default `~/.snorlax-bot`), not a desktop/iOS bundle or Settings picker. Empty/missing file = no MCP; built-ins still work. MCP tools join the existing runtime-owned OpenAI loop (auto-run, max 8, `kind=tool` persist + `tool.start`/`tool.done`). Built-in names win; MCP tools are namespaced `server__tool`. Shell still has no extra network. MCP HTTP is the runtime’s HTTP path (like `web_fetch`). Failed MCP server start does not block boot. Failed MCP tool still leaves a normal assistant follow-up (#34). LAN URLs may be loopback, RFC1918, or `.local`; reject `169.254.169.254`. See §13.

**v0.8 (CEO 2026-08-26):** Question widgets. Runtime-owned `ask_user_question` ends that agent turn. Persist `kind=widget` LEFT (same speaker as the agent) with `{ prompt, helpText?, options[1-6], allowCustom?, multiSelect?, dismissOnMoveOn? }` plus `widgetStatus` pending|resolved|dismissed and `widgetValues` on that same Message. SSE: persist, then `message.done` with the full Message, then the stream ends. No `widget.*` event. Answer is POST `{ widgetReply: { id, values?, dismissed? } }` — not a user-right bubble and not a new user Message. Dismiss does not wake the agent. A pick continues that POST as hop-0. One pending widget per transcript (409 if the model emits a second, or if the user POSTs content while pending unless `dismissOnMoveOn`). Isolation and thread-only channel widgets stand. Tools stay auto-run (no approval widget). See §14.

**v0.9 (CEO 2026-08-26):** Skills and cron routines. One routine = one agent + one SKILL.md slug. Skills load from `SNORLAX_DATA_DIR/skills/<slug>/SKILL.md`. Scheduler is runtime-owned; cron is 5-field in Asia/Taipei (fixed zone, not the Mac TZ). A due run writes a normal assistant Message in that agent's 1:1 (`senderId=A`, `kind=message`, optional `routineName` kicker). Not a hop, not a widget, not a channel post. Isolation stands: B never appears in A's 1:1. `GET /v1/agents/{id}/routines` lists `{ id, name, skill, schedule, enabled }` (404 missing agent; **409** channel). Pause/enable is `PATCH .../routines/{rid}` `{ enabled }` (404 unknown; **409** channel). POST seeds `{ name, skill, schedule }` (201; **422** unknown skill / bad cron / channel). No DELETE. Do not fire while paused. Missed ticks while the runtime was down are skipped (no catch-up storm). Chrome is list + enable/pause only on the agent info pane. Widgets OK if a run needs a decision (reuse v0.8). Seed channel DELETE 204 stands. OpenAPI **0.9.0**. See §15.

**v0.10 (CEO 2026-08-26):** MCP connect chrome. Runtime owns MCP (v0.7 `mcp.json` stdio + LAN). Clients never speak MCP or the model. `GET /v1/plugins` → `{ id, name, status: connected|needsAuth }` (bare array; catalog may be empty). Connect tap: `POST /v1/plugins/{id}/auth` → `{ authorizationUrl }` for the OS browser (desktop system browser; iOS `ASWebAuthenticationSession`); OAuth callback hits the runtime. No uninstall / delete / store / search API. No Add-custom UI (hand-edited `mcp.json` still works). Chat card is Message `kind=connect` (widget chrome family, **not** `kind=widget`). Same-row reply `connectReply { id }` or `{ dismissed: true }` — no user-right bubble. Isolation matches widgets. After Connected, tools auto-run. Plugins list is **Settings only** (runtime-global), not the agent pane. OpenAPI **0.10.0**. Isolation, report-back, widgets, cron, seed DELETE 204, Mac oMLX, no Mac folder picker stand. See §16.

**v0.11 (CEO 2026-08-26):** Assistant markdown. Clients render only. `Message.content` stays a **string**. Do not add `contentType`, mime, html, or `blocks[]`. Runtime does not rewrite assistant text to HTML or split one message into many. SSE `message.delta` is still text chunks of that same string. User messages stay plain text as stored. Keep widget / connect / tool / routine fields. No MCP mix-in. Assistant LEFT `kind=message` is **14px markdown** with **no grey agent bubble**, **16/14** headings, lists, bold. User-right stays **plain text** (`https://` tappable). Fenced code: **full turn width**, **8px radius**, elevated fill + 1px border, **12px muted language + Copy**, **12px/1.45** mono, horizontal scroll. `https://` links: accent, hover underline, OS browser. Inline code: **13px** mono, **4px** radius, accent 18% mix. Tool / widget / Connect are not markdown. Out of scope: mermaid, math/KaTeX, raw HTML (escape/strip; never inject HTML from model text). OpenAPI **0.11.0**. Isolation / report-back / seed DELETE 204 / Mac oMLX / no Mac folder picker / `workspaceTree.ts` (never reintroduce `computerPane.ts`) stand. See §17.

**v0.12 (CEO 2026-08-26):** MCP Add custom. Runtime owns MCP (v0.7 `mcp.json` stdio + LAN). Clients never speak MCP or the model. Settings Plugins (already Settings-only, runtime-global): **Add** sheet (320px on Settings; name + segmented `stdio` / `URL`) submits `POST /v1/plugins` `{ name, transport: "stdio" | "url", command?, args?, url? }` → **201** `{ id, name, status }`. **Remove** is `DELETE /v1/plugins/{id}` (204; uninstall plus disconnect; unknown 404). No separate disconnect endpoint. Do not auto-open a connect card. GET and `POST .../auth` stay as v0.10. `kind=connect` cards unchanged. No store, no search catalog. OpenAPI **0.12.0**. Isolation, report-back, widgets, markdown, cron, seed DELETE 204, Mac oMLX, no Mac folder picker, `workspaceTree.ts` (never reintroduce `computerPane.ts`) stand. See §18.

**v0.13 (CEO 2026-08-26):** Event listeners, webhook first. Runtime-owned. A routine has **either cron or a trigger, never both**. Result still fires as that agent's 1:1 LEFT `kind=message` as A, with `routineName` kicker (same as v0.9 cron). Isolation + report-back stand. B never paints in A's 1:1. Channel thread only if that's how cron already works — keep existing 1:1-as-A behavior. Every event-trigger routine has a URL + **Copy** in the agent identity pane row. An HTTP **POST** to that URL fires the routine (same SKILL.md / prompt path as cron). Slack/GitHub listeners only if that MCP plugin is **already connected**. Do **not** block this slice on Slack/GitHub. No new OAuth / connect chrome. If a plugin isn't connected, omit those trigger types or 422 — webhook must still work with zero plugins. Chrome: muted line shows the trigger (`Webhook` / `Weekdays 9:00`). Pause still works (PATCH enabled). **No New routine button** in chrome if v0.9 still has no create UI — seed/API-created routines are enough; pane gets Copy for webhook URL. Out: full Box/VNC, teach-a-task, marketplace. Desktop first, iOS match. OpenAPI **0.13.0**. See §19.

**v0.14 (CEO 2026-08-26):** Box computer preview. Runtime owns a **per-agent sandbox display** (1280×800). Clients never send clicks this slice. Clients never speak MCP or the model. `GET /v1/agents/{id}/computer` → `{ hasSandbox, width: 1280, height: 800, imageUrl }`. `imageUrl` is **Bearer PNG** at `GET /v1/agents/{id}/computer/screenshot` (same SNORLAX_TOKEN). Channel id → **409**. Missing agent 404. **No** click/key/scroll POST. File-tree workspace GETs stay as they are (v0.6 / `workspaceTree.ts`). Do **not** reintroduce `computerPane.ts`. Do **not** change the existing 320px file-tree column. Chrome is the **agent identity pane only** (desktop 320px overlay / iOS sheet), **above Routines**: 12px muted `Computer`, then a **16:10** shot (**288×180**, **8px** radius, **1px `--border`**). Letterbox the 1280×800 sandbox into that. Display only: default cursor, **no overlay, no Open**, no clicks. Live: poll GET computer / screenshot ~1–2s while the pane is open. No sandbox: omit `imageUrl`, **no frame**, 12px muted `No computer yet.` Otherwise the desktop is **always on**, even idle. iOS: same block, 16:10, 8pt radius, 1pt border, 12pt labels. **No tap-to-open.** Channel pane unchanged. Teach-a-task waits. OpenAPI **0.14.0**. See §20.

**v0.16 (CEO 2026-08-26):** Teach-a-task. **Desktop + runtime only.** iOS still is unchanged this slice — no Record, no Save-as-skill, no `ios/` UI (generated `/v1` types only if the OpenAPI bump requires). Spec rides this PR. OpenAPI **0.16.0**. **No second PR.** Record only exists **inside a desktop takeover session** (the v0.15 Open overlay). Preview `Open` and iOS still are unchanged. Out: blank New skill UI, edit generated markdown, marketplace, iOS record, VNC, a separate OS window. See §22.

**v0.17 (CEO 2026-08-26):** Create/delete routine chrome. One impl PR covering runtime + desktop + iOS. Spec rides this PR. OpenAPI **0.17.0**. **No second PR.** Agent identity pane **Routines** header gets trailing **12px `Add`** (same family as Plugins `Add`; iOS 12pt). Add sheet (same family as Add plugin / Save as skill, **320px**). Out of scope: marketplace catalog, iOS Open/Record/takeover, skill markdown editor, Slack/GitHub trigger builder. See §23.

**v0.18 (CEO 2026-08-26):** Skill markdown editor. One impl PR covering runtime + desktop + iOS. Spec rides this PR. OpenAPI **0.18.0**. **No second PR.** Agent identity pane **Skills** block **below Routines**. Computer / Routines / Add-routine unchanged. Channel pane unchanged. Out of scope: marketplace catalog, iOS Open/Record/takeover, `/` autocomplete, blank New skill (create stays v0.16 teach-a-task). See §24.

**v0.19 (CEO 2026-08-26):** iOS takeover. **iOS only** (`ios/`). One impl PR. Spec rides this PR. **No second PR.** Desktop Open/Done stays v0.15 — no `desktop/` chrome work. Runtime protocol **unchanged**: reuse v0.15 `POST/DELETE /v1/agents/{id}/computer/session` plus pointer/key. OpenAPI stays **0.18.0** (v0.15/v0.18 contract). `recording` stays unused. No iOS Record, pinch-zoom, marketplace, or a separate window. See §25.

**v0.20 (CEO 2026-08-27):** iOS Record. **iOS only** (`ios/`). One impl PR. Spec rides this PR. **No second PR.** Desktop Record/Stop/Save stays v0.16 — no `desktop/` chrome work. Runtime protocol **unchanged**: reuse v0.16 `POST/DELETE /v1/agents/{id}/computer/record` then `POST /v1/agents/{id}/skills { name }`. Discard is omit that POST. OpenAPI stays **0.18.0** unless docs-only bump. Out: marketplace, pinch-zoom, blank New skill, desktop changes. See §26.

**v0.21 (CEO 2026-08-27):** Composer **`/`** skill autocomplete. **1:1 agent composer only.** `/` at the **start of a token** opens the existing **`@`** typeahead overlay (not a second system): **240px / 240pt**, `--bg-elevated`, **1px** border, **8px / 8pt** radius, rows **36px** desktop / **44pt** iOS, name **14px / 14pt**, **no avatar**, filter by **name**. ↑↓ + Enter or click/tap inserts **`/name` as plain text** (not a chip, not an `@`). Esc closes. **Send** loads that agent’s **SKILL.md** into the turn. Unknown **`/foo`** stays plain text. Channel **`/`** is plain text — **no popup, no typeahead, no load path**. Empty list or no match: **no popup, no empty copy**. **No extra chip chrome** in the transcript. OpenAPI stays **0.18.0** — typeahead uses existing **`GET /v1/agents/{id}/skills`**. No second PR. Out: marketplace, blank New skill, `@skill`. See §27.

**v0.22 (CEO 2026-08-27):** Blank New skill. One impl PR covering runtime + desktop + iOS. Spec rides this PR. **No second PR. No new HTTP routes.** OpenAPI stays **0.18.0** (optional `body` on existing `POST /skills`). Skills header trailing 12px / 12pt `Add`. Empty `No skills yet.` still shows Add. 320px sheet title **`New skill`**. Primary `Add` disabled until name AND body. `POST { name, body }` writes SKILL.md (no capture). `POST { name }` omitted body stays record-to-skill. Out: marketplace, `@skill`, Slack/GitHub builder. See §28.

**v0.23 (CEO 2026-08-27):** Slack/GitHub inbound listeners. One impl PR covering runtime + desktop + iOS. Spec rides this PR. **No second PR. No new HTTP routes.** OpenAPI stays **0.18.0**. POST `/v1/agents/{id}/routines` stays one route, cron XOR trigger. Slack/GitHub segments on the Add-routine sheet **only if** that MCP plugin is `connected` (omit, do not disable; one 12px row). Slack field placeholder `#eng`, hint `Channel the bot is in.`; GitHub `owner/name`, hint `One repo. No wildcards.` Fire is that agent’s 1:1 as A. Out: marketplace, VNC, event picker. See §29.

**v0.24 (CEO 2026-08-27):** Curated plugin catalog (not a store). One impl PR covering runtime + desktop + iOS. Spec rides this PR. **No second PR.** OpenAPI stays **0.18.0**. New route only: `GET /v1/plugins/catalog` (Bearer). Bare array `{ id, name, transport, command?, args?, url? }`. Exactly two entries when neither is installed (Slack then GitHub). Catalog Add is existing `POST /v1/plugins` (no extra sheet). Omit a row when that kind is already in `GET /v1/plugins` (connected or needsAuth). Empty catalog is 200 `[]` (hide Catalog header). Custom Add stays. Out: public store, search, VNC, extra channel types. See §30.

**v0.25 (CEO 2026-08-27):** Chat attachments. One impl PR covering runtime + desktop + iOS. Spec rides this PR. **No second PR.** OpenAPI stays **0.18.0**. `POST /v1/agents/{id}/attachments` multipart field `file` → **201** `{ id, kind, name, url, size }` (`kind` is `image` or `file`). `url` is Bearer `GET /v1/attachments/{id}`. Over 10MB or `video/*` → **422**. Message POST adds `attachmentIds[]`; empty `content` is ok if that list is non-empty. Unknown/foreign ids → 422. GET message grows `attachments` (user-right only this slice). Runtime includes them in that turn. Existing `{ mime, data }` images stay off-model. No `/v1/chats/` resource. Composer + user-right only; agent-sent attachments and video wait. See §31.

**v0.26 (CEO 2026-08-27):** Agent-sent attachments. One impl PR covering runtime + desktop + iOS. Spec rides this PR. **No second PR.** OpenAPI stays **0.18.0**. No new client HTTP routes. GET `attachments` on any `kind=message` (user-right or agent LEFT); `kind=tool` / `widget` / `connect` stay `[]`. Runtime binds `write_file` and computer screenshot files onto that assistant `kind=message` (same store + Bearer GET). Composer and POST `/v1/agents/{id}/attachments` stay user-only. LEFT chrome matches user-right (images 220×160; files 36px name chips). Out: video, VNC, public store, extra channel types, agent drag-drop, public/unauthed URLs, a second upload route, agent-sent video. See §32.

**v0.27 (CEO 2026-08-27):** Video attachments. One impl PR covering runtime + desktop + iOS. Spec rides this PR. **No second PR.** OpenAPI stays **0.18.0**. No new HTTP routes. No `/v1/chats/` resource. Lift the v0.25 `video/*` 422. Same POST `/v1/agents/{id}/attachments` multipart `file` → **201** `{ id, kind, name, url, size }`. `kind` is `image` | `file` | `video`. Video max **50MB** (`Max 50MB.`); image/file stay **10MB** (`Max 10MB.`). Empty still 422. GET attachments on any `kind=message`. Runtime does **not** send video bytes to the model. Agent `write_file` of a video under 50MB may bind as `kind=video` on the assistant `kind=message`. Composer drop/pick video is a pending chip (not the v0.25 danger line). Bubble player **220×160**, native controls, no autoplay, user-right and agent LEFT. See §33.

**v0.28 (CEO 2026-08-27):** watch-video tool. One impl PR. Spec rides this PR. **No second PR.** OpenAPI stays **0.18.0**. No new client HTTP. No `/v1/chats/` resource. Desktop + iOS idle (no chrome; player stays the v0.27 220×160). Built-in `watch_video` `{ attachmentId }` auto-runs like write_file / shell / web_fetch. Agent decides when to call it. Unknown / foreign / non-video → tool error (POST messages stays 200). Success is plain text into the tool result; chrome is the existing 12px muted `kind=tool` line `Watched {name}`. Not a second player, not on the user-right, not a widget. Do not auto-inject every video. Bytes still never go into the user turn. See §34.

v0 still stands: local inference on DGX Spark, macOS desktop + thin iOS companion, no Cursor login. This spec adds two features on top of the chat loop.

## Problem

1. Chat reads as a log. Grok Bot (and iMessage) put the user on the right and everyone else on the left. Desktop and iOS must match.
2. Chinghau created a second agent and expected Grok-Bot-like teammate messaging. Isolated 1:1 transcripts mean agents cannot see or reach each other.

## Not in v0.1

No MCP, connectors, skills, sandbox/box GUI, computer-use screenshot pane,
routines, or question widgets. No copying a private 1:1 into a channel unless
the user asked to tell the room (an `@` mention is that ask). One seeded
channel plus extra user-created channels (v0.4). Basic file/shell/web tools
are **v0.5** (runtime-owned; not in the client). The thin desktop Computer
pane (file tree + text preview over that sandbox) is **v0.6**. Runtime MCP
(stdio + LAN, `mcp.json` on disk) is **v0.7**. Full sandbox
computer GUI (browser, VNC, terminal) stays later.

## 1. Chat layout (desktop + iOS)

- User messages: RIGHT. Distinct user fill. No avatar required on the right.
- Agent messages: LEFT, with that agent's avatar + display name on the first bubble of a streak.
- Consecutive bubbles from the same speaker stack. A speaker change starts a new streak.
- Streaming tokens render in-place in the left bubble.
- Do not show raw runtime cues (`[agent]`, `[Group chat:]`, `[routine]`) to the user.
- Alignment is a client rule: `author.kind == user` → right; anything else → left.
- A 1:1 never shows another agent's bubble, an involve line, or muted `to B` / `from A` chrome.

## 2. Surfaces

Two surfaces, both required.

### Group channel (shared transcript)

- Seed one channel named `Snorlax-Bot` (id `snorlax-bot-group`, kind `channel`). Every agent is a member. New agents auto-join it.
- The seed 1:1 is a **distinct** resource: id `snorlax-bot`, name **Snorlax**, title Assistant, kind `agent`. Never reuse `snorlax-bot` as the channel transcript.
- Sidebar lists both as normal rows. The channel row shows a muted “Channel” subtitle from `kind` (12px / 12pt), not by guessing id. Never two rows named “Snorlax-Bot”. Do not show ids in chrome.
- One shared channel transcript. User can read and send.
- User messages: right. Agent posts: left, identified.
- Posts are async. Nobody waits for a reply. Unmentioned members stay silent unless they have something material to add.
- **This is the display surface for agent–agent traffic.** If A talks to B, it appears here, not in A's 1:1 or B's 1:1.

### Agent 1:1 (user ↔ that agent only)

- A 1:1 with agent A (including Snorlax at `snorlax-bot`) shows **only** the user (right) and A (left).
- No other agent's bubbles. No involve line `{User} mentioned you in {A}`. No muted `to B` / `from A` DM chrome.
- GET/list and SSE must not paint peer messages into a 1:1. Filter: `senderId` is `user` or that agent. The channel is shared and is not filtered that way.

## 3. `@` syntax

- Mentions resolve by agent id from the typeahead chip. The composer sends `mentions: [id, ...]`.
- Unresolved `@text` (typed but not picked from typeahead) is NOT a mention. It stays plain text. Do not 422 the send just because someone typed `@foo`.
- **422 only for an unknown chip id** (and `@everyone` chip in a 1:1). Unknown/ambiguous `@text` in the body is omitted.
- No display-name prefix matching on user sends. Exact `@DisplayName` in the body may still route (same as a resolved chip).
- Multiple mentions are allowed on one send, bounded by the hop / peer-send caps in section 5. A one-mention-per-send cap is rejected.
- `@everyone` is valid only in a group channel (all members are addressed). Invalid as a chip in 1:1. Typed `@everyone` in a 1:1 is plain text.
- **Group channel:** the message is stored in that channel only. Mentioned agents are addressed and should reply. Unmentioned agents stay silent by default.
- **1:1 with agent A:** `@B …` stays in A's transcript as the user's bubble (right). A replies in that 1:1. B does **not** appear in A's 1:1. B does **not** get an involve bubble in B's 1:1. B is addressed in a **handoff thread** on the seeded channel (v0.2).
- Agent-authored `@Name` notifies that member the same way a user mention does. If that mention originates in a 1:1, the peer traffic still lands in the channel.
- Mentions are highlighted in the bubble. Composer offers `@` autocomplete of agent display names (desktop + iOS). Inserting a chip keeps a stable caret after the chip; the chip stays a chip.

## 4. What appears in which transcript

- User talks to A in 1:1 → only A's 1:1. User right, A left.
- User `@B` in A's 1:1 → A's 1:1 keeps the user-right chip (plus a jump line when a handoff was created) and A's reply. B does not appear in A's or B's 1:1. The seeded channel gets a LEFT `kind=handoff` root authored as A (`from {A}` + `userAsk`, brief collapsed as Context) and a flat thread of replies. Not a user-right copy in the channel.
- A talks to B (agent-authored `@B` or a routed mention) → the same channel thread, not A's 1:1 or B's 1:1. No jump chip unless the user caused the handoff.
- Anyone posts in the group channel → that channel transcript. Mentioned agents wake. 1:1s are unchanged unless the user is also chatting there separately.

Privacy: a 1:1 with the user is private. Do not relay it into a channel or to another agent unless the user `@` mentioned them or explicitly asked to tell them.

## 5. Loop / hop limits

Locked. Do not implement hop 0→1-only.

- User → current agent is hop 0.
- Max hop depth: 3 (A→B→C→D ok; D→E dropped).
- Max peer sends (DMs + channel posts) per originating user turn: 4.
- Same-edge cap: A→B→A once per turn; a second A→B in that turn is dropped.
- Dropped messages are silent in the UI. Runtime logs them. Sender is not prompted to retry.
- Delivery is always async. Recipients never block the sender.

These caps are unchanged in v0.1.1. Only the **display surface** for peer traffic changed: channel, not 1:1s.

## 6. Acceptance checks

Desktop (macOS) and iOS must each pass:

- User bubble is right; the current agent's bubble is left with name + avatar.
- In the seeded channel, a second agent's posts are left and labeled as them, never as the user.
- From agent A's 1:1, user sends `@B hello`. A's 1:1 shows the user (right), a jump line “Also in Snorlax-Bot” when `handoff` is set, and A (left) only. B's 1:1 is empty of that turn. The channel timeline shows a compact handoff row; the thread has A's card and B's reply.
- Scrolling a long 1:1 or channel transcript does not scroll the 256px agent list. Only the transcript pane scrolls.
- Right-click (desktop) or swipe/context (iOS) deletes a roster row after confirm `Delete {name}? This removes the agent and its chat.` (channel copy: `the channel and its chat`). That includes seed `snorlax-bot` and seed channel `snorlax-bot-group` (DELETE 204, no auto-reseed, never recreate). Delete is on the sidebar row, not in the info pane. After seed channel delete, if the roster is nonempty, select an **agent** first, else a remaining channel. Empty roster keeps chrome (sidebar, header, composer). No jump chip unless the target channel is still in the roster.
- `@` autocomplete lists agent names. Picking a chip keeps the caret after the chip. Unresolved `@text` stays plain text; send does not error.
- Streaming tokens stay left-aligned.
- Fourth hop is dropped (debug/test hook is enough).
- Two mentions on one user send both deliver (inside the peer-send cap).

iOS-only: agent list includes the seeded channel; user can read and send in 1:1 and in that channel over LAN.

## 7. Locked decisions (2026-08-25)

1. User `@Name` in a 1:1 is runtime-routed to that agent (not "model must remember to send").
2. One seeded `Snorlax-Bot` channel; all agents auto-join it **while that row exists**. Extra user-created channels are v0.4 (`POST /v1/agents` with `kind=channel`). If the seed channel is gone, new agents join nothing.
3. Hop limit 3, max 4 peer sends per user turn, same-edge once. Hop 0→1 / one-mention-per-send is rejected.
4. **v0.1.1:** Agent–agent traffic displays in a channel, not in the two 1:1 transcripts. 1:1 isolation overrides the earlier "DM-in-1:1s / involve bubble" rule. Seed `snorlax-bot` is the Snorlax 1:1 only. v0.4 1:1 @involves log on seed `snorlax-bot-group` when present (ignore body `channelId`); if gone, log on body `channelId` if it is an existing channel, else skip the log.
5. 422 only for an unknown typeahead chip id (and `@everyone` chip outside the group).
6. Roster names: seed 1:1 is **Snorlax**; channel is **Snorlax-Bot**. Clients show muted “Channel” from `kind`. Never two rows named Snorlax-Bot. Do not show ids in chrome.
7. Collaboration fluency is **v0.2** (handoff pack / channel thread / jump chip). v0.1.1 isolation stays.
8. **v0.3:** Header info pane. kind=agent is editable identity; kind=channel is a read-only member list. Seed agent DELETE is 204 and is not auto-reseeded. User-created channel DELETE is 204 (v0.4). Seed channel DELETE is 204 (CEO 2026-08-26).
9. **v0.4:** Report-back into A's 1:1 (not a hop; report as each peer lands). Extra channels with member editor. Isolation unchanged. CEO 2026-08-26: seed channel DELETE 204, no auto-reseed; A2A log is seed, else body `channelId` if it is a real channel, else skip.
10. **v0.5:** Runtime-owned tools in a workspace jail. Clients never call the model or the tools. No computer pane this slice. Four CEO locks: (1) channel project is a sandbox dir under `~/.snorlax-bot`, not a Mac folder picker; (2) shell has no extra network — HTTP is `web_search` / `web_fetch` only; (3) tools auto-run (no approval/question widgets); (4) search provider is env/config (`SNORLAX_SEARCH_PROVIDER` / `SNORLAX_SEARCH_URL`), not hardcoded.
11. **v0.6:** Thin Computer pane over the existing sandbox. Desktop 320px right column (collapsible, default open). iOS: no pane. Same roots as `workspace_for()`. Clients never call tools. No `projectPath` / folder picker. Isolation unchanged. Seed channel DELETE 204, no reseed. Seed PATCH identity 409 except `sharedProject`.
12. **v0.7:** FastAPI runtime is the MCP client. Desktop/iOS never speak MCP. `mcp.json` under `SNORLAX_DATA_DIR`. stdio + LAN HTTP/SSE. Namespaced `server__tool`; built-ins win. No marketplace UI this slice.
13. **v0.11:** Assistant markdown is client-only. `Message.content` stays a string. Do **not** add `contentType`, mime, html, or `blocks[]`. Runtime does not rewrite assistant text to HTML or split one message into many. SSE `message.delta` is still text chunks of that same string. User messages stay plain text as stored. Keep widget / connect / tool / routine fields. No MCP mix-in. Drop the grey agent bubble on LEFT `kind=message`. User-right stays plain. No mermaid, math, or raw HTML.
14. **v0.12:** MCP Add custom. Settings Plugins: 320px Add sheet posts `POST /v1/plugins` `{ name, transport: "stdio"|"url", command?, args?, url? }` (201). Remove is `DELETE /v1/plugins/{id}` (204; uninstall plus disconnect). No separate disconnect endpoint. Do not auto-open a connect card. GET and `POST .../auth` stay v0.10. `kind=connect` isolation unchanged. Runtime owns MCP. Clients never speak MCP. No store / search catalog. Keep v0.8 widgets, v0.9 routines, seed DELETE 204, content string. Do not reintroduce `computerPane.ts`.
15. **v0.13:** Event listeners, webhook first. Cron XOR trigger. GET adds `kind` (`cron` | `webhook` | `slack` | `github`); `schedule` only for cron; `webhookUrl` only for kind=webhook (secret token in the path; Copy; clients must not paint it); optional `label` for Slack/GitHub. Incoming fire is `POST {webhookUrl}` (no Bearer). 204 then the same 1:1 LEFT `kind=message` as cron. Paused or unknown token is 404 and does not run. Slack/GitHub 422 unless GET `/v1/plugins` shows that plugin `status=connected`. Do not emit Connect cards from the pane. Pause still PATCH `{ enabled }`. No New routine button. Isolation, report-back, widgets, markdown, MCP connect + Add custom, seed DELETE 204, Mac oMLX, no Mac folder picker, `workspaceTree.ts` stand.
16. **v0.14:** Box computer preview. Runtime owns a per-agent 1280×800 sandbox display. `GET /v1/agents/{id}/computer` `{ hasSandbox, width: 1280, height: 800, imageUrl }`. `imageUrl` is `GET /v1/agents/{id}/computer/screenshot` (Bearer, image/png). Channel 409. Missing agent 404. No click/key/scroll POST. Identity pane only, above Routines: 12px muted `Computer`, 288×180 16:10, 8px radius, 1px border, letterbox, default cursor, no Open, no overlay. No sandbox → omit imageUrl, `No computer yet.` no frame. Idle desktop still on. iOS match, no tap-to-open. Do not change the 320px file-tree column. Do not reintroduce `computerPane.ts`. Teach-a-task waits.
17. **v0.15:** Box takeover. Desktop + runtime only. iOS unchanged (preview only; no tap-to-open, no session POST). `POST /v1/agents/{id}/computer/session` 201 `{ sessionId }`; `DELETE .../session` or `DELETE .../session/{sessionId}` 204. While session exists: `POST .../pointer` `{ x, y, type }` and `POST .../key` `{ key, type, text? }` in 1280×800 (200). Agent tools that drive the sandbox while session exists → 409. Channel 409. Missing agent 404. No session + pointer/key → 409. GET `/computer` may include `driving: user|agent|idle`. Desktop: 12px muted `Computer` + trailing 12px `Open` when hasSandbox; 288×180 pointer cursor, click Opens. Empty `No computer yet.` has no Open. Overlay over chat + info pane (sidebar stays). Scrim `--bg`. 1280×800 letterboxed, 8px radius, 1px border. 52px bar: 24px avatar + name, 12px muted `You're driving · agent paused`, trailing primary `Done` 36px. Esc = Done. Composer inert. Agent does not run tools / drive the sandbox while the session is up. Done → display-only; preview last frame. File-tree column untouched. `workspaceTree.ts` (never reintroduce `computerPane.ts`). Teach-a-task waits. Out: recording, a separate OS window, VNC.
18. **v0.16:** Teach-a-task. Desktop + runtime only. iOS unchanged (no Record, no Save-as-skill, no `ios/` UI except generated `/v1` types). Record only inside a desktop takeover session. `POST /v1/agents/{id}/computer/record` → 201 `{ recording: true }` (409 without session / already recording); `DELETE .../record` → 204 (no SKILL.md; capture pending until save or discard); `POST /v1/agents/{id}/skills { name }` → 201 Skill `{ id, name }` writes SKILL.md (v0.9 load path). 422 empty name / no pending capture. Discard is skip this POST. GET computer may include `recording` when hasSandbox. Channel 409. Missing agent 404. Chrome: 12px muted `Record` left of primary `Done`; recording → 12px `--danger` `Stop` + 6px danger dot (static if Reduce Motion); Done disabled while recording; Esc while recording = Stop, else Done; Stop opens 320px `Save as skill` sheet (Name 14px, Save 36px disabled until name; × / Cancel discards). Capture records pointer/key + screenshot context so the skill is runnable via v0.9. Out: blank New skill UI, edit generated markdown, marketplace, iOS record, VNC, a separate OS window.
19. **v0.17:** Create/delete routine chrome. Runtime + desktop + iOS. Routines header trailing 12px `Add` (Plugins family; iOS 12pt). Empty still `No routines yet.` — Add still shows. 320px `Add routine` sheet: Name 14px; skill list is 44px rows from GET `/v1/agents/{id}/skills` `{ id, name }` (none: 12px `No skills yet.`); segmented Schedule/Webhook; cron placeholder `0 9 * * 1-5` plus 12px muted `Taipei. Weekdays 9:00 is 0 9 * * 1-5.`; webhook no extra fields. Primary Add 36px (iOS 44pt) disabled until name + skill + (cron if Schedule). Header × cancels. Rows 44px; Copy left of Remove on webhook; muted 12px `Remove` left of pause; `Remove {name}?`. GET skills empty 200 `[]`, channel 409. POST cron/webhook 201; Slack/GitHub trigger 422. `DELETE .../routines/{routineId}` → 204 (unknown 404; channel 409). Pause PATCH unchanged. Out: marketplace, iOS Open/Record/takeover, skill markdown editor, Slack/GitHub trigger builder.
20. **v0.18:** Skill markdown editor. Runtime + desktop + iOS. Skills block below Routines. 12px muted `Skills`. Empty `No skills yet.` Row 44px: name 14px, trailing 12px muted `Edit` then `Remove`. Confirm `Remove {name}?` then danger Remove / Cancel. **No blank Add** — create stays teach-a-task. Edit is a 320px sheet: title `Edit skill`; Name 14px; body textarea 12px/1.45 mono, min-height 200px — **source, not rendered preview**. Primary Save 36px / iOS 44pt, disabled until name and body. × discards. iOS matches (sheet + TextEditor, 12pt/1.45 mono, min-height 200pt, 44pt Save). List stays GET `/skills` `{ id, name }`. GET `/skills/{sid}` `{ id, name, body }` (`body` is the full SKILL.md source including YAML frontmatter plus recipe). PATCH `{ name, body }` 200 `{ id, name, body }` (write in place; prefer keep `id` stable). DELETE 204 (do not cascade-delete routines). Unknown sid 404. Channel 409. Empty name/body on PATCH 422. No blank POST. Out: marketplace, iOS Open/Record/takeover, `/` autocomplete, blank New skill.
21. **v0.19:** iOS takeover. iOS only. Desktop Open/Done stays v0.15. Reuse v0.15 `POST/DELETE /v1/agents/{id}/computer/session` plus pointer/key. `recording` unused. Computer header 12pt `Open` when hasSandbox; 16:10 shot also Opens. Empty `No computer yet.` has no Open. Open is full-screen (not a sheet). Safe-area 52pt bar: 24pt avatar + name, 12pt muted `You're driving · agent paused`, trailing 12pt `Keyboard` then primary `Done` 44pt. Keyboard focuses a hidden field → `POST …/key`. Letterboxed 1280×800: tap is click, pan is move, no pinch, swipe-back disabled. Done = DELETE session, back to the still. Composer covered. Channel pane unchanged. No Record. OpenAPI stays 0.18.0.
22. **v0.20:** iOS Record. iOS only. Desktop Record/Stop/Save stays v0.16. Reuse v0.16 `POST/DELETE /v1/agents/{id}/computer/record` then `POST /v1/agents/{id}/skills { name }`. Discard is omit that POST. Record only inside a takeover session. 12pt muted `Record` left of primary `Done`; Keyboard stays trailing (v0.19). Recording: 12pt `--danger` `Stop` plus a 6pt danger dot (static if Reduce Motion); Done disabled while recording. **No Esc** — Stop is the only way out of record. Stop opens `Save as skill` sheet (Edit skill family; Name 14pt; Save 44pt disabled until name; × / Cancel discards — no SKILL.md). Save writes that agent's skill (v0.18 list, no extra chrome) and returns to takeover with `Record` muted again. Keyboard / tap-click / pan-move / Done from v0.19 stay. Channel 409. OpenAPI stays 0.18.0.
23. **v0.21:** Composer `/` skill autocomplete. **1:1 agent composer only.** `/` at the start of a token (not only the start of the message; `https://` is not a trigger) opens the existing `@` typeahead overlay — do not invent a second typeahead system. **240px / 240pt** above the composer, `--bg-elevated`, **1px** border, **8px / 8pt** radius. Rows **36px** desktop / **44pt** iOS. Name **14px / 14pt**. **No avatar.** Filter by name as they type. ↑↓ + Enter or click/tap inserts `/name` as **plain text** (not a chip, not an `@`). Esc closes. Sending that message loads that agent’s SKILL.md into the turn. Unknown `/foo` stays plain text. Channel `/` is plain text — no popup, no typeahead, no load path. Empty skill list or no match: **no popup, no empty copy**. No extra chip chrome in the transcript. Typeahead list is existing `GET /v1/agents/{id}/skills`. No new HTTP. OpenAPI stays 0.18.0. Out: marketplace, blank New, `@skill`. Never reintroduce `computerPane.ts`.
24. **v0.22:** Blank New skill. Runtime + desktop + iOS. Skills header trailing 12px / 12pt `Add` (Routines / Plugins family). Empty `No skills yet.` — Add still shows. Rows keep Edit / Remove. 320px sheet title **`New skill`** (not “Add skill”). Name 14px / 14pt. Body 12px/1.45 mono textarea (iOS TextEditor 12pt/1.45), min-height 200px / 200pt — **source, not preview**. Primary `Add` 36px desktop / 44pt iOS, disabled until name AND body. × discards — writes nothing. Same POST `/v1/agents/{id}/skills`, two bodies: `{ name, body }` writes SKILL.md (no capture) → **201** `{ id, name }`; empty name or empty body → **422**; `{ name }` omitted body stays the record path (pending capture; 422 without). Channel **409**. GET/PATCH/DELETE unchanged. `/` typeahead unchanged. OpenAPI stays **0.18.0**. Out: marketplace, `@skill`, Slack/GitHub builder. Never reintroduce `computerPane.ts`.
25. **v0.23:** Slack/GitHub inbound listeners. Runtime + desktop + iOS. Same POST `/v1/agents/{id}/routines`, cron XOR trigger. Slack `{ type: slack, channel }` → **201** `kind=slack` `label="Slack {channel}"` (no webhookUrl). GitHub `{ type: github, repo }` (`owner/name`, no wildcards) → `kind=github` `label="GitHub {repo}"`. Unconnected plugin, empty channel/repo, or wildcards → **422**. GET omits those rows unless the plugin is `connected`. Pause skips. Fire via the connected MCP into that agent’s 1:1 as A (Slack = messages in that channel; GitHub = pr-opened / pr-pushed / pr-merged). Add-routine sheet: Slack/GitHub segments only when connected (one 12px row; omit, do not disable). Slack 14px `#eng` + 12px `Channel the bot is in.`; GitHub `owner/name` + `One repo. No wildcards.` Primary Add 36px / 44pt. Copy stays webhook-only. No event picker. No Connect CTA. OpenAPI stays **0.18.0**. Out: marketplace, VNC, extra channel types. Never reintroduce `computerPane.ts`.
26. **v0.24:** Curated plugin catalog (not a store). Runtime + desktop + iOS. New route only: `GET /v1/plugins/catalog` (Bearer, runtime-global) → bare array `{ id, name, transport, command?, args?, url? }` (omit nulls). Curated in runtime code. Exactly two entries when neither is installed, Slack then GitHub (`id` `slack` / `github`). Bake a real stdio or url transport that existing `POST /v1/plugins` accepts (npx stdio is fine). Omit a row when `GET /v1/plugins` already has a connected or needsAuth plugin whose id or name matches that kind (same needle as `plugin_kind_connected`). Both installed → 200 `[]`. Empty catalog is 200 `[]` (hide Catalog header; do not 404). No POST /catalog. No uninstall-from-catalog. `POST /v1/plugins` unchanged. `GET /v1/plugins` still `{ id, name, status }` only. Settings: 12px muted `Catalog` below custom rows; 44px Slack/GitHub trailing 12px `Add`; custom Add sheet stays. OpenAPI stays **0.18.0**. Out: public store, search, VNC, extra channel types, paid plugins. Never reintroduce `computerPane.ts`.
27. **v0.25:** Chat attachments. Runtime + desktop + iOS. `POST /v1/agents/{id}/attachments` (Bearer, multipart field `file`) → **201** `{ id, kind, name, url, size }`. `{id}` is the same conversation as `/messages` (kind=agent 1:1 or kind=channel). Missing 404. `kind` is `image` for `image/*` (not video), else `file`. `url` is Bearer `GET /v1/attachments/{id}` on this host. Over 10MB → 422 `Max 10MB.`. `video/*` → 422 `Video isn’t supported yet.`. Empty file 422. Message POST adds `attachmentIds[]`; empty `content` is ok if that list is non-empty (image-only / file-only send). Unknown or foreign ids → 422 (do not 404 the send). GET message includes `attachments` on **user-right only**; assistant rows are `[]`. Runtime includes them in that turn (image kinds as `image_url`; `text/*` as filename + extracted text; else a short note). Legacy `MessageCreate.images: [{ mime, data }]` stay off-model. Composer + user-right only. Paperclip stays; desktop also drop onto the composer (accent 1px highlight). iOS paperclip: Photos or Files. Pending chips wrap above the bar, 6px gap. Image pending **56×56** + 20px ×. File pending **36px** chip, 13px name, 12px muted size, ×. Send is on if text or any chip. Over 10MB / video: 12px `--danger` line, send off. User-right images **220×160**; files 36px name chips (8px radius, 1px border, 13px) above the text; chip opens the Bearer URL. Channel and 1:1 both. `@` and `/` unchanged. iOS chips 44pt hit. OpenAPI stays **0.18.0**. No `/v1/chats/` resource. Out: agent-sent attachments, video, public/unauthed URLs, a second upload route, VNC. Never reintroduce `computerPane.ts`.
28. **v0.26:** Agent-sent attachments. Runtime + desktop + iOS. No new client HTTP routes. OpenAPI stays **0.18.0**. GET `attachments: [{ id, kind, name, url, size }]` on any `kind=message` (user-right or agent LEFT). `kind=tool`, `widget`, and `connect` stay `[]`. Composer and `POST /v1/agents/{id}/attachments` plus `MessageCreate.attachmentIds` stay user-only. Do not add an agent upload route. After a turn, if the agent used `write_file` or a computer screenshot, the runtime copies those files onto that assistant `kind=message` (same attachment table + Bearer `GET /v1/attachments/{id}`). Screenshot → `kind=image`. `write_file` → `kind=file`, or `kind=image` if the written path is `image/*`. Skip `video/*` and over 10MB. Bind onto the `kind=message`, never the tool-trace row. LEFT chrome matches user-right: images **220×160**; files 36px name chips (8px radius, 1px border, 13px) **above** the markdown, left-aligned, **6px** gap (iOS **6pt**; 36pt chips, 44pt hit); chip opens the Bearer URL. Empty `attachments[]`: no extra chrome. Do **not** paint on tool lines, widgets, Connect cards, or the channel timeline handoff card. Isolation: attachments ride the message (B’s assistant row is not in A’s 1:1). Channel thread: speaking agent’s message only. Out: video, VNC, public store, extra channel types, agent drag-drop, public/unauthed URLs, a second upload route, agent-sent video. Never reintroduce `computerPane.ts`.
29. **v0.27:** Video attachments. Runtime + desktop + iOS. No new HTTP routes. OpenAPI stays **0.18.0**. Lift v0.25 `video/*` 422 (`Video isn’t supported yet.`). Same POST `/v1/agents/{id}/attachments` multipart `file` → **201** `{ id, kind, name, url, size }`. `Attachment.kind` is `image` | `file` | `video`. `image/*` (not video) → image, max 10MB (`Max 10MB.`). `video/*` or `.mp4`/`.mov`/`.m4v`/`.webm`/`.avi`/`.mkv` → video, max 50MB (`Max 50MB.`). else → file, max 10MB. Empty still 422. GET `/v1/attachments/{id}` Bearer unchanged. `MessageCreate.attachmentIds` may include video ids; empty content ok if the list is non-empty. GET attachments on any `kind=message`. `kind=tool` / `widget` / `connect` stay `[]`. Runtime MUST NOT send video bytes to the model (no transcription, no in-model watch; `apply_to_user_content` skips `kind=video`). Agent `write_file` of a video under 50MB may bind as `kind=video` on the assistant `kind=message`. Composer: video drop/pick is a pending chip (not the v0.25 danger line); over 50MB is 12px `--danger` `Max 50MB.`, send off. Bubble player **220×160**, native controls, no autoplay, poster at that size; user-right and agent LEFT on `kind=message` only (same placement as images). Chip/player opens Bearer GET. Isolation unchanged. Out: VNC, public store, extra channel types, transcription / watch-video tool, public/unauthed URLs, a second upload route. Never reintroduce `computerPane.ts`.
30. **v0.28:** watch-video tool. Runtime. Desktop + iOS idle (no chrome). No new client HTTP. OpenAPI stays **0.18.0**. No `/v1/chats/`. Built-in `watch_video` `{ attachmentId }` (required) on the existing auto-run tools list. Resolve on this conversation only (1:1 agent id or channel id). Unknown or foreign id → tool result `Error: unknown attachment id` (do not 422 the user POST). `kind != video` → `Error: not a video`. Success is plain text (filename + short description, cap ~8k), never JSON, never raw/base64 video bytes. `done_summary`: `Watched {name}` / `watch_video failed`. Chrome is that existing 12px muted `kind=tool` line under the agent’s LEFT streak. Not a second player, not on the user-right, not a widget, no Watch button. Player stays v0.27 **220×160**. Do not auto-call / auto-inject every video. `apply_to_user_content` still skips video bytes (short `user attached {name}` stub; may include `attachmentId`). Description is local: ffprobe duration/size + a few ffmpeg frames captioned by the same local OpenAI-compat backend when vision is available; if frames/caption fail, name + duration/size is still success. Out: VNC, public store, extra channel types, live camera, client transcode UI, a new chrome surface, a new HTTP resource. Never reintroduce `computerPane.ts`.

## 8. v0.2 — collaboration handoff (locked)

When a handoff happens (user `@chip` in A's 1:1, or A DMs B):

- A still answers in the 1:1 (user + A). Looping B does not suppress that.
- Pack B wakes with (not a quote): `{ originating: { id, name, title }, userAsk, brief, mentionedIds }`.
- `brief` = last 8 messages of that 1:1, user+A only, cap 2k chars, drop oldest.
- Agent DM: `userAsk` = A's text; `brief` = last 8 of A's 1:1.

### Channel thread

On `snorlax-bot-group`. Root is a LEFT handoff card authored as A (muted “from {A}” + `user_ask`; brief collapsed as Context). Flat thread under that root; later hops stay here. B/A post LEFT; user in the thread is RIGHT. Unread on the channel row.

GET channel messages must **not** dump thread replies onto the timeline. Timeline = handoff roots (and other unreplied-to roots). `?threadId=` (or `replyTo`) returns that thread.

### Jump chip (only when the USER caused the handoff)

Muted 12px / 12pt system line under the user-right bubble that has the `@chip`. Copy “Also in {channel name}” (accent on the name; seed is Snorlax-Bot). Not a bubble, no avatar, no B text. Whole line tappable → opens the channel at that thread. No `handoff` field → no chip. Second `@B` in that 1:1: reuse the same thread, new jump line under that bubble. Hop/cap silent drop → no `handoff` field, no chip.

Where: A's 1:1 = user, A, jump chip. B's 1:1 unchanged. Peer lives in the thread.

### API (additive)

Keep `senderId` / `hop` / `mentions`. Add `replyTo` on channel msgs. Originating user message gets `handoff: { channelId, threadId }` (no fake speaker). `kind: handoff` on the channel root. 422 still unknown chip id.

### Chrome

Channel timeline: one compact LEFT row per handoff: A's 20px avatar + name, muted “from {A}”, one-line `user_ask`, muted “N replies”. Tap opens the thread. Thread: back to timeline; root LEFT card (A's avatar + name, muted from A, `user_ask`, collapsed Context). Replies under it, same L/R. Unread = 6px accent dot on the channel roster row. Desktop first, iOS match.

Not in v0.2: widgets, box, tools, MCP, skills, extra channels. Do not reopen 1:1 isolation.

## 9. v0.3 — agent identity + info pane (locked)

Header click (whole header row: 24px / 24pt avatar + name) opens an info pane. Desktop also **Cmd+Shift+I**. Close is **X** on the pane. Opening the pane does not change the 1:1 transcript. Desktop: **320px overlay on the chat**, not a third column. iOS: sheet.

**Agent vs channel are different panes.** Never treat `snorlax-bot-group` as an agent profile.

### Agent pane (`kind=agent`, including seed Snorlax)

72px / 72pt avatar (initials from name if empty), 16px / 16pt name, 13px / 13pt muted title + description. Gear beside X edits those four. Save is `PATCH { name, title, description, avatar }` to `/v1/agents/{id}`. Id is not editable. Empty avatar → initials. Avatar is a PATCH string or null (data URI or persisted `/v1/images/{id}`); no second upload API.

CEO override: seed `snorlax-bot` DELETE is **204**, gone from GET, no auto-reseed. Empty roster is fine. CEO override 2026-08-26: channel `snorlax-bot-group` DELETE is **204**, gone from GET, no auto-reseed — never recreate that id. Empty roster is OK (no agents and/or no channels) and **keeps chrome**. Delete is sidebar right-click / iOS swipe on the **row**, **not** in the info pane. Seed channel row gets the same Delete as other rows. After seed agent delete, if the roster is only the channel, select **Snorlax-Bot** — do not fake a seed. After seed channel delete, if anything remains, select an **agent** first, else a remaining channel. No jump chip if no channel remains. Seed identity PATCH is allowed. Seed channel identity PATCH stays 409. User-created: PATCH + DELETE as today.

### Channel pane (`kind=channel`)

**Seed `snorlax-bot-group`:** Read-only identity. Display name + muted Channel + member list from `memberIds` (avatar + name + title each). No gear, no PATCH name, no avatar editor. 44px / 44pt member rows, not tappable in this slice. All agents auto-join; DELETE 204 (CEO 2026-08-26), no auto-reseed, never recreate. Delete is sidebar/swipe on the row, not in this pane.

**User-created channel:** Same member rows. Gear beside X edits `name` + `memberIds` (`PATCH { name, memberIds }`). Seed pane stays read-only.

Header keeps showing live name/title/avatar after PATCH. Roster updates too. 1:1 is still user+A only.

Not in v0.3: computer preview (no placeholder), routines list, connector/Channels row, per-agent notifications, shape/color beyond avatar, create-agent wizard, sidebar sections.

## 10. v0.4 — report-back + extra channels (locked)

Keep isolation: GET/transcript of A's 1:1 is still only user + A. B does not appear as B. No involve bubble, no “from Test” speaker in the 1:1. Jump chip “Also in {channel}” may remain as an optional inspect link under the user's @ message only; it is **not** required to get the answer.

### Report-back

1. User `@chip` B in A's 1:1 (typeahead id). Runtime wakes B with the existing handoff pack (`originating`, `userAsk`, brief last 8, `mentionedIds`) in a **seed** `snorlax-bot-group` thread when that channel exists (body `channelId` ignored). If the seed is gone and the 1:1 POST body has `channelId` that is an existing `kind=channel` row, log there. Else skip the log (no `handoff` field, no jump chip, no fake seed). Unknown/stale `channelId`: skip, do not 422, do not recreate seed. B still wakes (not written to B's 1:1); A still reports in A's 1:1 as A (non-hop, `senderId=A`, no `from` field). Extra channels are a separate create flow; 1:1 involves never open a new channel per mention.
2. B's system/wake: you were asked to **do** the task. First reply is the work product. Do not acknowledge that you can. Do not ping-pong. Prompt/task only — **no ACK classifier** in code (do not detect “I can help”). FYI with no ask → stay silent (`looks_like_ask` is FYI silence, not an ACK retry).
3. When B's channel-thread turn completes (`message.done`) **or B is hop/cap dropped**, enqueue a generation for A on A's 1:1. This is **not** a peer send. It does **not** increment hop or spend hop budget. Hop caps only apply to A2A/channel peer sends.
4. Prompt to A (not a Message field): `{ result, from: { id, name, title }, threadId, userAsk }`. Hop-drop: `result` is a miss so A still tells the user B wasn't reached.
5. Persist a normal assistant Message in A's 1:1: `role=assistant`, `senderId=A`, `senderName`/`avatar`=A, **no mentions of B**, **no `from` field on the Message**. Isolation: never write B into A's 1:1. B's 1:1 stays empty.
6. Multiple `@chips`: fire A's report as **each** B lands (don't wait-for-all). Serialize A's report-back gens on that 1:1 so tokens don't interleave (insert the report-back as the next job after that peer).
7. Hold the originating 1:1 POST SSE open until every mentioned B has done or been dropped **and** A has reported for each. Extra `message.delta` / `message.done` on the **same** stream. No new SSE event type.
8. Chrome: report-back is another of A's **LEFT** bubbles in the 1:1 (20px avatar+name, no from B). Jump chip only under the user's @ message. Hop-drop is still A's LEFT bubble, no system chrome. Do not duplicate `from {Name}:` inside bubble body.
9. Channel thread remains the A2A log. Timeline = handoff roots; `?threadId=` = thread. `@mention` inside an extra channel stays in **that** channel's thread.

### Extra channels

- Seeded `snorlax-bot-group` / Snorlax-Bot stays on first empty DB. Auto-joins all agents. PATCH 409. DELETE 204, no auto-reseed, never recreate (CEO 2026-08-26). No member editor on seed this slice. After that delete, clients select an agent first if any remain, else a remaining channel.
- User can create additional channels: `POST /v1/agents` `{ name, kind: "channel", memberIds }` → 201. Omit kind → still create agent. `memberIds` are agent ids only; 422 unknown id / channel id in the list / missing name. Omitted memberIds snapshot every current agent. New agents auto-join `snorlax-bot-group` only if that row still exists; if the seed channel is gone they join nothing. Not extras.
- User-created channel PATCH `{ name, memberIds }` → 200 (add/remove members). DELETE 204. Agent DELETE (including seed `snorlax-bot`) stays 204 from v0.3. No new upload route.
- Desktop + iOS: `+` opens a **160px / 160pt** menu: New agent / New channel. New channel is a **320px overlay / iOS sheet**: name + member picker (44px rows, 28px avatar, checkbox, agents only). User-created channel pane has a gear; seed pane stays read-only. Delete is sidebar/swipe on every row including the seed channel; not in the info pane. Empty roster keeps chrome. No jump chip if the target channel is gone.

Not in v0.4: widgets, box, MCP, skills, vision. Tools land in v0.5.

## 11. v0.5 — basic tools (locked)

Match Grok Bot’s working tools (files, shell, web), not the full computer GUI
/ live preview. Clients never call the model or the tools. The runtime runs
OpenAI-compat function calling against oMLX/vLLM (mock in CI) and continues
the completion. Cap **8** tool rounds.

CEO locks (2026-08-26), do not reopen this slice:

1. **Channel project is a sandbox dir** under `~/.snorlax-bot` (or
   `SNORLAX_DATA_DIR`). It is **not** a picker for a folder on the host Mac.
   Extra fields such as `projectPath` are ignored.
2. **Shell has no extra network.** HTTP is `web_search` / `web_fetch` only.
   Do not curl/wget from shell.
3. **Tools auto-run.** The runtime executes tool calls immediately. No
   approval widget, no question widget, no “allow this command?” this slice.
4. **Search provider is configuration** (`SNORLAX_SEARCH_PROVIDER`, optional
   `SNORLAX_SEARCH_URL` with `{query}`), not a hardcoded vendor in the loop.

### Workspaces

- Agent workspace: `~/.snorlax-bot/workspaces/agents/{agentId}/` (or under
  `SNORLAX_DATA_DIR`). Created on first tool use. Isolated from the host home
  directory.
- Channel project: **always** `~/.snorlax-bot/workspaces/channels/{channelId}/`
  (or under `SNORLAX_DATA_DIR`). Members share that sandbox. Created lazily
  on first tool use in that channel (seed + user-created). Never a host-folder
  picker.
- 1:1 turns: tools run in that agent’s workspace.
- Channel / handoff thread turns: tools run in the channel sandbox (created
  on first tool use).
- Path jail: file tools cannot escape the active workspace root. Shell cwd
  is that root; `HOME` is the workspace, not the host home. Do not pass
  Docker/SSH host secrets into the tool env.

### Built-in tools

`list_dir`, `read_file`, `write_file`, `delete_file`, `shell` (no extra
network), `web_search` (configured provider; default DuckDuckGo HTML, no
paid key), `web_fetch` (GET, text/markdown, size cap). HTTP is the two web
tools only.

### Prompting

Agents know they have these tools. Tools run immediately — do not ask the
user to approve. Do not ACK ping-pong. Write code to files; do not dump a
whole app in the chat bubble. If B needs A’s files, they must be in a
channel sandbox or A must put them there. 1:1 isolation means B cannot see
A’s private workspace.

### API / UI

Additive. `/v1` chat contract stays. SSE `tool.start` / `tool.done` with
`{ id, name, summary, ok?, senderId, senderName }`. Tool activity also
**persists** as Message `kind=tool` with short content (“Read src/a.ts”,
“Ran ls”, “Fetched …”), same `senderId` as the speaking agent, so GET
matches live SSE. Not JSON, not a user-right bubble. Rows live only in the
transcript where that agent is speaking (1:1 if A tools; channel thread if
B tools). A’s 1:1 report-back is a normal assistant `kind=message` as A and
**must not** copy B’s tool lines. After any tool round the turn always
finishes with a normal assistant `kind=message` (streamed and persisted),
even if tools failed, the model returned empty text, or inference errored
after tools already ran — do not end on only a `kind=tool` line or
`event:error` with no saved assistant bubble. The final answer is a normal
LEFT bubble.
Unknown events must be ignored. No computer preview, no file-browser API,
no folder/path field (`sharedProject` is the boolean toggle). iOS sees chat
and those status lines only — **no file browser** this slice.

Not in v0.5: MCP, browser-use GUI, computer screenshot pane, widgets, host
folder picker, extra shell network, iOS file browser. Seeded channel DELETE
is 204, no auto-reseed (main / #32). Seed identity PATCH stays 409.

## 12. v0.6 — computer pane (locked)

Thin file browser over the **existing** tool sandbox. Not full B4 (no real
browser, no per-agent VNC screen, no 128GB Spark VM). Not MCP. Not question
widgets. Not a Mac folder picker.

Chat stays primary. Desktop gains a **320px RIGHT computer pane** (same width
language as the identity overlay, but this one is a **pane next to chat**,
not a 320px overlay). It can collapse. Default open on desktop. Collapse
control is in the chat header. Sidebar stays 256px. Identity overlay (320px)
still overlays chat, not a fourth column. Do not steal transcript scroll.
Do not turn the app into a VNC client.

iOS: no computer pane this slice (chat + muted tool traces only; lag
allowed).

The pane shows the active conversation’s workspace, same root as runtime
`workspace_for()`:

- 1:1 → `~/.snorlax-bot/workspaces/agents/{agentId}/`
- Channel with `sharedProject` true → `workspaces/channels/{channelId}/`
- Channel with `sharedProject` false → first member agent’s workspace
  (speaking/selected is unclear on a GET). Do not invent a Mac path.

Empty workspace is a fine empty state (no fake files). Click a text file to
preview in the pane. Directories expand. Path jail identical to tools (no
`..`, no abs escape). Binary: show a short “binary / too large”, not a hex
dump. No terminal GUI, no screenshot stream, no take-over vs watch-only
(watch-only implied: user browses files; agents still mutate via tools).

Clients never call tools or the model. New GETs are runtime reads of the
sandbox, not a second tool loop. Isolation unchanged: the pane never shows
another 1:1’s files. B’s channel workspace is not painted inside A’s 1:1
pane.

### API (additive, camelCase `/v1`)

- `GET /v1/agents/{id}/workspace?path=` → `{ root, path, entries: [{ name, kind: file|dir, size? }] }`
- `GET /v1/agents/{id}/workspace/file?path=` → `{ path, content, truncated? }`
  404 if missing, 422 if escape / binary policy.

Auth: same bearer. 404 conversation. Path default `.`. Size cap similar to
`read_file` (256k). `root` is sandbox-relative (`workspaces/agents/{id}`),
never a host Mac path. No `projectPath`, `folderPath`, or picker. No new SSE
required; desktop may refetch after `message.done` / `tool.done`. Unknown
events ignored.

Seed `snorlax-bot-group` DELETE stays 204, no reseed. Seed PATCH identity
409 except `sharedProject`. Hop / report-back / tool loop / PR #34 follow-up
stay.

Not in v0.6: MCP marketplace UI, browser-use GUI, screenshot stream, terminal GUI,
take-over, iOS file browser, host folder picker, question widgets. Runtime MCP
client is **v0.7**.

## 13. v0.7 — MCP client (locked)

The FastAPI runtime is the MCP client. Desktop and iOS never speak MCP, never
call tools, and never call the model.

CEO locks (2026-08-26):

1. **stdio and LAN.** Connect a local stdio MCP server *and* an MCP server on
   the same LAN (HTTP/SSE or streamable HTTP). Do not assume MCP must be on
   the public internet.
2. **Config on disk** under `SNORLAX_DATA_DIR` (default `~/.snorlax-bot`),
   e.g. `mcp.json`. Not in the desktop/iOS bundle. No marketplace UI, no
   Settings picker this slice. Empty or missing file = no MCP; built-ins
   still work.
3. **Same tool loop.** MCP tools join the existing runtime-owned OpenAI loop
   (auto-run, max 8 rounds, `kind=tool` persist + `tool.start`/`tool.done`).
4. **Built-in names win.** `list_dir`, `read_file`, `write_file`,
   `delete_file`, `shell`, `web_search`, `web_fetch` are never replaced.
   Prefix MCP tools `server__tool`.
5. **Shell still has no extra network.** MCP HTTP is the runtime’s HTTP path
   (like `web_fetch`), not the agent shell.
6. **Boot and follow-up.** If an MCP server fails to start, the runtime still
   boots and built-ins work (log the failure). After a failed MCP tool, still
   leave a normal assistant follow-up (same as #34).
7. **LAN URLs.** Allow loopback, RFC1918, `.local`. Do not require a public
   hostname. Reject obvious SSRF to link-local metadata (`169.254.169.254`).

### Config

```json
{
  "mcpServers": {
    "example-stdio": { "command": "python3", "args": ["path/to/server.py"], "env": {} },
    "example-lan": { "url": "http://127.0.0.1:8765/mcp" }
  }
}
```

Optional `"transport": "sse"` (or a URL path ending in `/sse`) selects legacy
SSE; otherwise a `url` is streamable HTTP.

### Chrome

Reuse muted 12px `kind=tool` lines (`Used server__tool` / `Ran …`). No new
computer preview. iOS unchanged except it already paints `kind=tool`.

Not in v0.7: question widgets, VNC/browser GUI, Mac folder picker, marketplace
UI, public-cloud MCP requirement. Computer pane, isolation, report-back, hop
budget, seed channel DELETE 204, seed identity PATCH 409 except
`sharedProject`, and PR #34 follow-up after failed tools all stay.

## 14. v0.8 — question widgets (locked)

Runtime owns the card. Clients render only and never invent fields. Never a
second event type. Never a user-right answer bubble.

### Protocol

- Message `kind=widget`, LEFT, same speaker as the agent.
- Body on that row: `{ prompt, helpText?, options[1-6]: { label, value?
  (default label), description?, style?: default|primary|danger },
  allowCustom?, multiSelect?, dismissOnMoveOn? }` plus `widgetStatus`
  `pending` | `resolved` | `dismissed` and `widgetValues` (empty while
  pending) on the same Message. Clients render only — they must not invent
  a widget payload.
- SSE: persist, then `message.done` with the full Message, then the stream
  **ends**. No `widget.*`. Unknown events still ignorable.
- Answer is POST `{ widgetReply: { id, values?: string[], dismissed?: true } }`
  on that same transcript. Updates the **same** Message (resolved or
  dismissed). Do not insert a user-right bubble. `values` are option.value
  (or custom text if allowCustom). multiSelect: several values; else 422 if
  more than one. Unknown id / already closed: 422.
- Dismiss = decline: **do not wake the agent**. Pick: wake the agent with
  the values (continuation SSE on that POST).
- One pending widget per transcript: **409** if the model emits a second, or
  if the user POSTs normal content while pending unless `dismissOnMoveOn`
  (then auto-dismiss, no agent wake for the widget, then run the new user
  message).
- Isolation: A's 1:1 GET/SSE only user + A, including only A's widgets. B
  must not widget into A's 1:1. Channel widgets are thread-only (`replyTo`
  set); omit `kind=widget` from the timeline GET. Report-back stays a
  normal assistant `kind=message` as A — never copy B's widget rows.
- Tools stay auto-run. Widgets are not tool-approval cards and do not gate
  shell / web / MCP.
- OpenAPI **0.8.0**. Phrase `prompt` as a natural question, not a menu
  instruction.

### Chrome (desktop first, iOS match, no extra sheet)

- LEFT streak of the speaking agent (20px avatar + 13px name on streak
  start, 4px / 16px gaps). Not a bubble. Not a user-right bubble.
- Card: min 240px max 320px, 12px radius, 12px padding, 1px `--border`,
  `--bubble` fill. Prompt 14px / 1.4. Optional helpText 12px muted under it.
- Options stack, gap 6px, 8px radius, left-aligned 13px / 500. Desktop
  height 36px; iOS 44pt. default = border + hover `--bg-hover`. primary =
  accent 28% mix (same tint as user bubbles). danger = `--danger` label, no
  extra icons. Optional description 12px muted under the label. Max 6, one
  column.
- allowCustom: 36px field (iOS 44pt) under the options, placeholder
  `Or type your own`. multiSelect: 16px checkbox on the left, `Done`
  primary full-width under the list.
- Dismiss: 20×20 muted × top-right; no confirm. Composer stays on.
- Resolved: **same card**. Picked row(s) get a trailing 16px accent check;
  the rest mute to 50%. Dismissed: options disabled, 12px muted `Dismissed`
  under the prompt.
- One pending card per transcript.

Out of scope: box/VNC, skills, routines, MCP marketplace UI, computer pane
changes, Mac folder picker. Do not reintroduce `computerPane.ts` next to
`ComputerPane.tsx` (use `workspaceTree.ts`).

## 15. v0.9 — skills and cron routines (locked)

Scheduler is runtime-owned. Chrome is list + enable/pause only. No create
UI this slice.

### Runtime

- Skill = `SKILL.md` under `SNORLAX_DATA_DIR/skills/<slug>/SKILL.md`
  (default `~/.snorlax-bot/skills/<slug>/SKILL.md`) with YAML frontmatter
  `name` + `description`, then markdown body. No trigger of its own. No
  marketplace / client picker.
- One routine = one `agentId` + one skill slug. SQLite. Fields: `id`,
  `name`, `skill` (slug), `schedule`, `enabled`. Cron is 5-field,
  **Asia/Taipei** (fixed zone, not the Mac TZ). No Slack/GitHub/event
  listeners. No teach-a-task. No HTTP DELETE.
- Scheduler ticks inside the FastAPI process. If enabled, a matching
  cron minute wakes that agent with the skill body (not persisted as a
  user Message) and writes a **normal assistant Message** in that
  agent's 1:1: `role=assistant`, `senderId=A`, `kind=message`, optional
  `routineName` for the 12px kicker. Not a hop, not a widget, not a
  channel post. Isolation + report-back stand: never paint B in A's 1:1.
  Do not run while paused. Missed ticks while the runtime was down are
  skipped (no catch-up storm).
- `GET /v1/agents/{id}/routines` lists `{ id, name, skill, schedule,
  enabled }` (bare array; schedule is 5-field cron; optional
  `scheduleLabel` is a display string). Missing agent is **404**.
  `kind=channel` is **409**.
- Pause/enable is `PATCH /v1/agents/{id}/routines/{rid}` `{ enabled }`
  (resume = enable). Unknown routine is **404**. Channel is **409**.
- POST may seed a routine for tests (`{ name, skill, schedule }` → 201).
  **422** unknown skill / bad cron / channel id. Clients have no
  create/delete this slice.
- Tools still auto-run. Widgets OK if that run needs a decision (reuse
  v0.8 `kind=widget`; no second protocol). Seed channel DELETE **204**
  stands. OpenAPI **0.9.0**.
- Humanized `{skill} · {Weekdays 9:00}` is a client concern.

### Chrome (desktop + iOS, same PR)

- Agent info pane only (320px overlay / iOS sheet), under the 72px
  identity. Channel pane unchanged. Do not touch Computer pane. Do not
  reintroduce `computerPane.ts` (`workspaceTree.ts` stays).
- 12px muted header `Routines`, then rows. Row 44px, name 14px/500, 12px
  muted line `{skill} · {humanized Taipei schedule}` e.g.
  `status · Weekdays 9:00`.
- Trailing enable/pause switch, 44px hit, **LIVE on the read pane** (not
  behind the gear). Paused: name at 50% opacity. Empty: 12px muted
  `No routines yet.`
- Run lands in that agent's 1:1 as a normal LEFT bubble. If `routineName`
  is set, 12px muted kicker under the sender name. Isolation stands.
- iOS matches, no extra sheet. No New / create / edit / delete UI, no
  teach-a-task, no marketplace, no skill picker.

Out of scope: box/VNC, teach-a-task, MCP marketplace UI, event listeners,
computer pane changes, Mac folder picker, create/edit/delete routine UI.

## 16. v0.10 — MCP connect chrome (locked)

Runtime owns MCP. Clients never speak MCP or the model. One protocol:
`kind=connect` is the widget chrome family, **not** `kind=widget`.

### HTTP

- `GET /v1/plugins` → bare array `{ id, name, status: connected|needsAuth }`.
  Catalog may be empty. Runtime-global.
- Connect tap: `POST /v1/plugins/{id}/auth` → `{ authorizationUrl }` for
  the OS browser; OAuth callback hits the runtime (GET on Spark :8787,
  or POST `/v1/plugins/oauth/callback` with `code` + `state`).
- No uninstall / delete / store / search API this slice. No Add-custom UI
  (v0.7 `mcp.json` still works for hand-edited stdio/URL).

### Chat card

- Message `kind=connect` (LEFT, same speaker as the agent). Body
  `{ prompt, helpText?, pluginId }` plus `connectStatus`
  pending|connected|dismissed on that Message.
- SSE: persist, then `message.done`, stream ends. No `connect.*` event
  on emit. Unknown SSE events remain ignorable.
- Same-row reply: `connectReply { id }` or `{ dismissed: true }`. No
  user-right bubble. Dismiss does not wake and does not return a URL.
  `{ id }` stays pending until auth finishes and emits `connect.url`
  `{ url, pluginId }` then ends; the client opens the OS browser. The
  OAuth callback (GET on the runtime, or POST complete with code+state)
  PATCHes that Message to `connectStatus=connected` and continues the
  agent turn so tools auto-run (no approval widgets). If the plugin is
  already connected, `{ id }` continues this POST as hop-0.
- Sending the card ends the turn until connect or dismiss. Content while
  pending is **409**.
- Isolation matches widgets: A's 1:1 only A's cards; channel **thread
  only**, never the timeline; B never paints in A's 1:1.

### Chrome (desktop + iOS, same PR)

- Connect card: LEFT streak, 240–320px, 12px radius/padding. Prompt 14px
  e.g. `Connect Slack to read your channels.` Optional 12px muted help
  `Opens your browser to sign in.` One primary `Connect` 36px / iOS 44pt.
  Dismiss × 20×20. Composer stays. Resolved: **same card**, 12px muted
  `Connected` or `Dismissed`.
- Plugins list: **Settings only** (runtime-global), **not** the agent pane.
  12px muted `Plugins`, 44px rows, name 14px, 12px muted `Connected` /
  `Needs sign-in`. Needs-sign-in trailing `Connect` (same browser). Empty:
  `No plugins yet.` No store, no search, no uninstall. iOS Settings match,
  no extra sheet.
- Auth: desktop OS browser; iOS `ASWebAuthenticationSession`.

Out of scope: Box/VNC, marketplace, computer pane, `computerPane.ts`
(helpers stay `workspaceTree.ts`). OpenAPI **0.10.0**. Isolation,
report-back, widgets, cron, seed DELETE 204, Mac oMLX, no Mac folder
picker stand.

## 17. v0.11 — assistant markdown (locked)

Clients render only. No second protocol PR. No MCP mix-in.

### Backend

`Message.content` stays a **string**. Do **not** add `contentType`, mime,
html, or `blocks[]`. The runtime does **not** rewrite assistant text to
HTML or split one message into many. SSE `message.delta` is still text
chunks of that same string (`{ id, role: assistant, delta, senderId,
senderName, senderAvatar }`). User messages stay plain text as stored.
Keep `kind=widget` / `kind=connect` / `kind=tool` / `routineName` intact.

### Chrome (desktop + iOS, same PR)

- Assistant LEFT `kind=message` (from A): markdown, **no grey agent
  bubble**. **14px / 14pt** body, **16/14** headings, lists, bold.
  `@` mentions still overlay.
- Links: accent, hover underline (desktop), **https only**, OS browser /
  Safari. Reject `javascript:` / `data:` / `http:`.
- Inline code: **13px / 13pt** mono, **4px / 4pt** radius, accent 18% mix
  (mention-chip tint).
- Fenced code: **full turn width**, **8px / 8pt** radius,
  `--bg-elevated` (iOS elevated fill) + **1px / 1pt** border, **12px /
  12pt** muted language + **Copy**, **12px / 12pt / 1.45** mono,
  horizontal scroll.
- User-right stays **plain text** (no markdown). `https://` URLs are
  tappable. Tool / widget / Connect are **not** markdown.
- Streamed `message.delta` re-renders the same LEFT markdown in place
  (close an unclosed fence so the buffer does not flash).

### Out of scope

Mermaid diagrams, math/KaTeX, raw HTML (escape/strip; never inject
HTML from model text). No Box GUI, marketplace, event listeners, or
create/delete routine UI. Do not touch MCP connect chrome except where
shared bubble styling intersects.

OpenAPI **0.11.0**. Isolation, report-back, widgets, cron, connect
chrome, seed DELETE 204, Mac oMLX, no Mac folder picker stand. Do not
reintroduce `computerPane.ts` (helpers stay `workspaceTree.ts`).

## 18. v0.12 — MCP Add custom (locked)

Runtime owns MCP. Clients never speak MCP or the model. One impl PR
covering runtime + desktop + iOS. Spec rides this PR. OpenAPI **0.12.0**.
Do not mix full Box, teach-a-task, event listeners, or a marketplace
catalog.

### HTTP (runtime owns it)

Existing:

- `GET /v1/plugins` → bare array `{ id, name, status: connected|needsAuth }`
- `POST /v1/plugins/{id}/auth` → `{ authorizationUrl }`; unknown id 404

Add:

- `POST /v1/plugins` `{ name, transport: "stdio" | "url", command?, args?: string[], url? }` → **201** `{ id, name, status: connected|needsAuth }`.
  - stdio: `command` required (`args` optional). **422** if name or command missing.
  - url: `url` required. Allow https and http (LAN). **422** if missing/invalid.
  - Invalid transport combo **422**.
- `DELETE /v1/plugins/{id}` uninstalls (disconnect + drop from catalog). **204**. Unknown **404**. Do not auto-open a connect card. No separate disconnect endpoint.

GET `/v1/plugins` and `POST /v1/plugins/{id}/auth` stay as v0.10. `kind=connect` isolation unchanged. Keep v0.8 widgets, v0.9 routines, seed DELETE 204, content string.

Do not add store/search catalog APIs. Auth is still the OS
browser (`POST /v1/plugins/{id}/auth` → `{ authorizationUrl }`; desktop
system browser; iOS `ASWebAuthenticationSession`). `kind=connect` cards
are **unchanged**. Tools still auto-run after connect. No approval
widgets.

### Chrome (Settings only, not the agent pane)

Keep the existing Plugins list: 12px muted `Plugins`, trailing 12px `Add`
(not a plus-catalog), 44px rows, name 14px, 12px muted `Connected` /
`Needs sign-in`. Connected = status only. needsAuth keeps trailing
`Connect`. Empty still `No plugins yet.`

Add:

- Trailing **Add** opens a **320px sheet on top of Settings** (same
  family as New channel). Title `Add plugin`. Name 14px. Segmented
  `stdio` / `URL` (same control as Theme). stdio: `Command` +
  `Arguments` (placeholders `npx`, `-y package`). URL: `Server URL`
  (`https://`). Primary `Add` 36px, disabled until name + command or
  URL. Header × cancels. Save posts `POST /v1/plugins` then refreshes.
- Every row: 12px muted `Remove`. Confirm copy exactly:
  `Remove {name}? This disconnects it.` then danger `Remove` / `Cancel`.
- iOS Settings match: header 12pt `Add`, rows 44pt, 12pt muted
  `Remove` + same confirm copy. Add is a sheet on Settings (not a
  store). Primary `Add` 44pt, disabled until name + command or URL. No
  extra sheet beyond Add.

Desktop first, iOS match. Connect card chrome does not change.

### Out of scope

Full Box/VNC, teach-a-task, event listeners, marketplace catalog /
public plugin store. Isolation, report-back, widgets, markdown, cron
routines, seed DELETE 204, Mac oMLX, no Mac folder picker, and
`workspaceTree.ts` (never reintroduce `computerPane.ts`) stand.

OpenAPI **0.12.0**.

## 19. v0.13 — event listeners, webhook first (locked)

Runtime-owned. One impl PR covering runtime + desktop + iOS. Spec rides
this PR. OpenAPI **0.13.0**. Do not mix full Box/VNC, teach-a-task, or a
marketplace catalog. No second protocol PR.

### Product lock

- A routine has **either cron or a trigger, never both**. Result still
  fires as that agent's 1:1 LEFT `kind=message` as A, with `routineName`
  kicker (same as v0.9 cron).
- Isolation + report-back stand. B never paints in A's 1:1. Channel
  thread only if that's how cron already works — keep existing 1:1-as-A
  behavior.
- **Webhook first:** every event-trigger routine has a URL + **Copy** in
  the agent identity pane row. An HTTP **POST** to that URL fires the
  routine (same SKILL.md / prompt path as cron).
- Slack/GitHub listeners only if that MCP plugin is **already
  connected**. Do **not** block this slice on Slack/GitHub. No new OAuth
  / connect chrome. If a plugin isn't connected, omit those trigger types
  or 422 — webhook must still work with zero plugins.
- Chrome: muted 12px line is `Webhook` / `Weekdays 9:00` / connected
  Slack-GitHub `label`. Webhook rows: 12px muted Copy left of the
  switch, then Copied for 1.5s. Do not paint the URL. No New / edit /
  delete. No Connect CTA in the pane.
- Out: full Box/VNC, teach-a-task, marketplace. Desktop first, iOS match.

### HTTP (runtime owns it)

Existing v0.9:

- `GET /v1/agents/{id}/routines`
- `POST /v1/agents/{id}/routines`
- `PATCH /v1/agents/{id}/routines/{routineId}` (`enabled` / pause)

Extend Routine (and create) so a routine is **cron XOR trigger**:

- GET: `kind` is `cron` | `webhook` | `slack` | `github`. `schedule`
  (Asia/Taipei 5-field) is present **only** for cron. `webhookUrl` is
  present **only** for `kind=webhook` (full URL for Copy; clients must
  **not** paint it). Optional `label` for Slack/GitHub (e.g.
  `"Slack #eng"`, `"GitHub owner/repo"`). Null fields are omitted.
- cron create: existing `{ name, skill, schedule }`.
- webhook create: `{ name, skill, trigger: { type: "webhook" } }` →
  **201** with `webhookUrl`. Secret token lives in the path, not query.
  POST of an event routine without a trigger is **422**. POST with both
  `schedule` and `trigger` is **422**.
- Incoming webhook: `POST {webhookUrl}` (`/v1/hooks/{token}`). No
  Bearer; the token is in the URL. If enabled, run that skill as that
  agent and persist assistant 1:1 `senderId=A`, `routineName` set.
  Success **204**. If paused or unknown token: **404**, do not run. No
  catch-up.
- Slack/GitHub: `{ type: slack|github, label? }` is **422** unless
  `GET /v1/plugins` shows that plugin `status=connected`. GET omits
  Slack/GitHub rows when that plugin is not connected. Inbound
  Slack/GitHub delivery is not in this slice — use webhook. Do not emit
  Connect cards from the pane.

Keep list + enable/pause. Still no create/delete chrome. Clients may POST
in tests. No DELETE this slice. Channel still 409.

### Chrome (agent identity pane, 44px rows)

- 44px row unchanged: name 14px, trailing enable/pause.
- Muted 12px line is `Webhook` or the Taipei cron (`Weekdays 9:00`) or,
  if that MCP plugin is already connected, `Slack #eng` /
  `GitHub owner/repo`.
- Webhook rows only: 12px muted `Copy` **left of the switch**. Tap
  copies `webhookUrl` (not shown on the row). Then `Copied` for 1.5s.
  Do not paint the URL. Do not add a second sheet.
- Slack/GitHub rows: no Copy. No Connect CTA in the pane — Connect
  cards stay in chat / Settings. Unconnected: omit those kinds.
- Pause stays. No New / edit / delete.
- iOS match: 44pt rows, 12pt muted line, 12pt muted Copy left of the
  switch, pasteboard + Copied 1.5s. Do not paint the URL. No extra
  sheet.

### Out of scope

Full Box/VNC, teach-a-task, marketplace catalog / public plugin store.
Isolation, report-back, widgets, markdown, MCP connect + Add custom,
seed DELETE 204, Mac oMLX, no Mac folder picker, and `workspaceTree.ts`
(never reintroduce `computerPane.ts`) stand.

OpenAPI **0.13.0**.

## 20. v0.14 — Box computer preview (locked)

Runtime owns a **per-agent sandbox display** (1280×800). One impl PR
covering runtime + desktop + iOS. Spec rides this PR. OpenAPI **0.14.0**.
Clients never send clicks this slice. Clients never speak MCP or the
model. Teach-a-task waits. **No second PR.**

### HTTP (runtime owns it)

- `GET /v1/agents/{id}/computer` → `{ hasSandbox, width: 1280,
  height: 800, imageUrl }`. `imageUrl` is present **only** when
  `hasSandbox` is true.
- Screenshot is **Bearer PNG** at `imageUrl`: `GET
  /v1/agents/{id}/computer/screenshot` (same SNORLAX_TOKEN as other
  GETs, `image/png`). Live capture of the runtime-owned 1280×800
  framebuffer. Idle desktop still returns a 1280×800 shot (always on).
  Do not ship a fake decorative PNG for agents that have a sandbox.
- Channel id → **409**. Missing agent **404**.
- If that agent has **no sandbox**: `hasSandbox: false` and omit
  `imageUrl` (clients show `No computer yet.`).
- **No** click/key/scroll POST this slice.
- File-tree workspace GETs stay as they are (v0.6 Computer pane /
  `workspaceTree.ts`). Do **not** reintroduce `computerPane.ts`. Do
  **not** change the existing 320px file-tree column this slice.

### Chrome (agent identity pane only)

Desktop 320px overlay / iOS sheet, **above Routines**. Channel pane
unchanged.

- 12px muted `Computer`, then a **16:10** shot (**288×180**, **8px**
  radius, **1px `--border`**). Letterbox the 1280×800 sandbox into that.
- Display only: default cursor, **no overlay, no Open**, no clicks.
- Live: paint whatever screenshot the runtime last sent (poll GET
  computer / image at a modest interval, e.g. ~1–2s while the pane is
  open; do not spin when closed).
- If that agent has **no sandbox**: **no frame**, 12px muted
  `No computer yet.`
- Otherwise the desktop is **always on**, even idle (still show the
  last/idle screenshot).
- iOS: same block in the agent sheet, 16:10, 8pt radius, 1pt border,
  12pt labels. **No tap-to-open.** No extra sheet.

Run result / tools / widgets / connect / markdown / routines / MCP Add
custom unchanged.

### Out of scope

User takeover, teach-a-task, a separate Box window, VNC, click/key POST.
Isolation, report-back, seed DELETE 204, Mac oMLX, no Mac folder picker,
and `workspaceTree.ts` (never reintroduce `computerPane.ts`) stand.

OpenAPI **0.14.0**.

## 21. v0.15 — Box takeover (locked)

**Desktop + runtime only.** One impl PR. Spec rides this PR. OpenAPI
**0.15.0**. **No second PR.** iOS still is unchanged this slice — no
tap-to-open, no session POST, no `ios/` UI. Teach-a-task waits. Out:
teach-a-task, recording, a separate OS window, VNC.

Computer block (agent identity 320px overlay, above Routines) already
shows a 288×180 letterboxed 1280×800 screenshot (v0.14). This slice
adds **Open / Done** on **desktop only**.

### HTTP (runtime owns it)

- Existing `GET /v1/agents/{id}/computer` + Bearer PNG screenshot
  unchanged. GET may include `driving`: `user` | `agent` | `idle` when
  `hasSandbox` (omit when no sandbox, like `imageUrl`).
- `POST /v1/agents/{id}/computer/session` → **201** `{ sessionId }`
  (takeover session). Idempotent POST returns the same `sessionId`.
- `DELETE /v1/agents/{id}/computer/session` → **204** (Done).
  `DELETE …/computer/session/{sessionId}` also **204**; unknown
  `sessionId` → **404**.
- While session exists: `POST …/pointer` `{ x, y, type }` and
  `POST …/key` `{ key, type, text? }` in 1280×800 coords (**200**).
  Pointer `type`: `move` | `down` | `up` | `click`. Key `type`: `down`
  | `up` | `type`. When `type` is `type`, `text` is preferred if present.
- Agent tools that drive the sandbox (`computer_click`, `computer_key`)
  while session exists → **409**. POST `/messages` to that agent is
  also **409** (agent paused).
- Channel id → **409** on these routes.
- Missing agent 404. No session + pointer/key → 409. No sandbox + POST
  session → 404.

### Chrome (desktop only)

- Computer header: 12px muted `Computer` + trailing 12px `Open` when
  `hasSandbox`. The 288×180 shot is **pointer cursor**; click also
  Opens. Empty `No computer yet.` has **no Open**.
- Open is an **overlay over chat + info pane** (sidebar stays). Scrim
  `--bg`. 1280×800 letterboxed to fit, 8px radius, 1px border.
- 52px bar: 24px avatar + name, 12px muted `You're driving · agent
  paused`, trailing primary `Done` 36px. **Esc = Done**.
- Pointer and keys map to sandbox coords (1280×800). **Composer is
  inert** so keystrokes don’t leak into chat.
- Agent does **not** run tools / drive the sandbox while the session
  is up.
- Done returns to display-only; preview picks up the last frame.
- Channel pane unchanged. Isolation / report-back / widgets / markdown
  / routines / MCP / seed DELETE stand. No Mac folder picker.
  `workspaceTree.ts` (never reintroduce `computerPane.ts`). File-tree
  column untouched.

### iOS

**No change.** Preview only. No tap-to-open. No session POST.

OpenAPI **0.15.0**.

## 22. v0.16 — teach-a-task (locked)

**Desktop + runtime only.** One impl PR. Spec rides this PR. OpenAPI
**0.16.0**. **No second PR.** iOS still is unchanged this slice — no
Record, no Save-as-skill, no `ios/` UI (generated `/v1` types only if
this OpenAPI bump requires). Preview `Open` and iOS still are
unchanged. Record only exists **inside a desktop takeover session**
(the v0.15 Open overlay).

Out of scope: blank New skill UI, edit generated markdown, marketplace,
iOS record, VNC, a separate OS window.

### HTTP (runtime owns it)

- Record only inside a takeover session.
- `POST /v1/agents/{id}/computer/record` → **201** `{ recording: true }`.
  **409** without session / already recording.
- `DELETE /v1/agents/{id}/computer/record` → **204** Stop. Does **not**
  write SKILL.md. Capture stays pending until save or discard (next
  record / session end discards).
- `POST /v1/agents/{id}/skills { name }` → **201** Skill `{ id, name }`
  writes `SKILL.md` under that agent's v0.9 skills dir
  (`SNORLAX_DATA_DIR/skills/<id>/SKILL.md`) from the pending capture.
  **422** empty name / no pending capture. Discard is skip this POST.
- GET computer may include `recording` (boolean) when `hasSandbox`.
  Omitted when no sandbox.
- Channel id → **409**. Missing agent 404.
- Capture records pointer/key (and enough screenshot context:
  `start.png` / `end.png` next to SKILL.md) so the skill is actually
  runnable later via v0.9 (`computer_click` / `computer_key` in the
  body). Do not ship an empty stub SKILL.md.
- Takeover session 201/204 and pointer/key routes stay (v0.15). Keep
  the v0.9 skill load path. No marketplace, no markdown editor API.

### Chrome (desktop only)

Chrome — 52px takeover bar:

- 12px muted `Record` **left of** primary `Done`.
- Recording: that control is 12px `--danger` `Stop` plus a **6px
  danger dot** (static if Reduce Motion). **Done is disabled** while
  recording.
- Esc while recording = Stop. Esc otherwise still Done.
- Stop opens a **320px sheet** (same family as Add plugin). Title
  `Save as skill`. Name 14px. Primary `Save` 36px, **disabled until
  name**. × / Cancel discards — **no SKILL.md**. Save writes that
  agent's skill (v0.9 list, no extra chrome) and returns to takeover
  with `Record` muted again. Optional 1.5s 12px `Saved` on the bar.

### iOS

**No change.** Preview only. No Record. No Save-as-skill.

Isolation, report-back, widgets, markdown, MCP, routines, seed DELETE
204, Mac oMLX, no Mac folder picker, `workspaceTree.ts` (never
reintroduce `computerPane.ts`) stand.

OpenAPI **0.16.0**.

## 23. v0.17 — create/delete routine chrome (locked)

One impl PR covering runtime + desktop + iOS. Spec rides this PR.
OpenAPI **0.17.0**. **No second PR.**

Out of scope: marketplace catalog, iOS Open/Record/takeover, skill
markdown editor, Slack/GitHub trigger builder.

### Product lock

- Agent identity pane **Routines** header gets trailing **12px `Add`**
  (same family as Plugins `Add`). iOS match (12pt).
- Add sheet (same family as Add plugin / Save as skill, **320px**):
  - Title `Add routine`.
  - Name 14px.
  - Skill list: **that agent’s `SKILL.md`** as **44px rows** (v0.9
    `GET /v1/agents/{id}/skills` `{ id, name }`). None: 12px
    `No skills yet.` and Add stays disabled.
  - Segmented `Schedule` / `Webhook` (same control as Theme / Add
    plugin stdio-URL).
  - Schedule: a cron field, placeholder `0 9 * * 1-5`, plus 12px muted
    `Taipei. Weekdays 9:00 is 0 9 * * 1-5.`
  - Webhook: no extra fields; after save the row is `kind=webhook`
    with existing **Copy** (never paint the URL).
  - Primary `Add` 36px (iOS 44pt) disabled until name + skill + (cron
    if Schedule).
  - Header × cancels.
- Empty still `No routines yet.` — Add still shows.
- Rows stay 44px: name 14px, trailing enable/pause. Muted 12px line is
  `Webhook` / Taipei cron (`Weekdays 9:00`) as v0.13. Webhook rows
  keep **Copy left of Remove**, then the pause switch.
- Every row gets muted **12px `Remove` left of the pause switch** +
  confirm copy **`Remove {name}?`** then danger Remove / Cancel.
- **Pause stays.** No edit-in-place this slice.

### HTTP (runtime owns it)

- `GET /v1/agents/{id}/skills` → array `{ id, name }` from that
  agent’s SKILL.md. Empty is **200 `[]`**. Channel **409**.
- POST `{ name, skill, schedule }` cron **201**; `{ name, skill,
  trigger: { type: "webhook" } }` **201** with `webhookUrl`.
- **422** missing name/skill, unknown skill, bad cron, both
  schedule+trigger. Slack/GitHub trigger still **422** (no builder
  this slice).
- **`DELETE /v1/agents/{id}/routines/{routineId}` → 204.** Unknown
  404. Channel 409.
- Pause PATCH `{ enabled }` unchanged. Keep webhook inbound and cron
  fire as v0.13. No skill markdown editor API.

### Chrome (desktop + iOS)

- Desktop: identity overlay. iOS: same Add sheet + Remove + confirm
  in the agent sheet. Channel pane unchanged.
- Slack/GitHub kinds remain list-only if the plugin is connected (no
  builder UI).

Isolation, report-back, widgets, markdown, MCP, computer
preview/takeover/record, seed DELETE 204, Mac oMLX, no Mac folder
picker, `workspaceTree.ts` (never reintroduce `computerPane.ts`)
stand.

OpenAPI **0.17.0**.

## 24. v0.18 — skill markdown editor (locked)

One impl PR covering runtime + desktop + iOS. Spec rides this PR.
OpenAPI **0.18.0**. **No second PR.**

Out of scope: marketplace catalog, iOS Open/Record/takeover, `/`
autocomplete, blank New skill (create stays v0.16 teach-a-task).

### Product lock

- Agent identity pane **Skills** block **below Routines**. Computer /
  Routines / Add-routine unchanged. Channel pane unchanged.
- Chrome:
  - 12px muted `Skills`. Empty: `No skills yet.`
  - Row 44px: name 14px, trailing 12px muted `Edit` then `Remove`.
  - Confirm: `Remove {name}?` then danger `Remove` / `Cancel`.
  - **No blank Add** — create stays teach-a-task.
- Edit is a **320px sheet**. Title `Edit skill`. Name 14px. Body is a
  textarea, **12px/1.45 mono**, **min-height 200px** — **source, not
  rendered preview**. Primary `Save` 36px / iOS 44pt, **disabled until
  name and body**. × discards.
- iOS matches (sheet + TextEditor, 12pt/1.45 mono, min-height 200pt,
  44pt Save). No extra sheet beyond Edit.

### HTTP (runtime owns it)

- List stays `GET /v1/agents/{id}/skills` `{ id, name }` (v0.17). No
  `body` field on the list.
- `GET /v1/agents/{id}/skills/{sid}` → `{ id, name, body }` (`body` is
  the full SKILL.md **source** including YAML frontmatter plus recipe,
  not a rendered preview).
- `PATCH /v1/agents/{id}/skills/{sid} { name, body }` → **200**
  `{ id, name, body }`. Writes the file in place. Keep the slug /
  skill `id` unless a name change truly requires a new slug — prefer
  keep `id` stable.
- `DELETE /v1/agents/{id}/skills/{sid}` → **204**. Does not
  cascade-delete routines that still name the skill (a later 422 on
  use is ok).
- **No blank POST** for a new empty skill — create stays teach-a-task
  `POST /skills { name }` from a capture.
- Unknown sid 404. Channel 409. Empty name/body on PATCH 422.

Isolation, report-back, widgets, markdown, MCP, computer
preview/takeover/record, routines Add/Remove, seed DELETE 204, Mac
oMLX, no Mac folder picker, `workspaceTree.ts` (never reintroduce
`computerPane.ts`) stand.

OpenAPI **0.18.0**.

## 25. v0.19 — iOS takeover (locked)

**iOS only** (`ios/`). One impl PR. Spec rides this PR. **No second PR.**
Desktop Open/Done stays **v0.15** — no desktop chrome changes. Runtime
protocol **unchanged**: reuse v0.15
`POST/DELETE /v1/agents/{id}/computer/session` plus pointer/key.
OpenAPI stays **0.18.0** (v0.15/v0.18 contract). `recording` stays
unused.

Out of scope: marketplace, iOS Record, pinch-zoom, a separate window,
desktop changes.

### Product lock

iOS Computer block (agent sheet, above Routines) already shows the
16:10 still (v0.14). This slice adds **Open** matching desktop.

Chrome:

- Computer header: **12pt `Open`** when `hasSandbox`; the **16:10 shot
  is tappable and also Opens**. Empty `No computer yet.` has **no Open**.
- Open is **full-screen (not a sheet)**.
- Safe-area **52pt bar**: 24pt avatar + name, 12pt muted `You're driving
  · agent paused`. Trailing **12pt `Keyboard`** then primary **`Done`
  44pt**.
- `Keyboard` focuses a hidden field so the system keyboard maps to
  `POST …/key`.
- Letterboxed 1280×800: **tap is click**, **pan is move**. **No pinch**.
  Swipe-back disabled.
- Done = `DELETE` session, back to the still.
- Composer is covered, not used.
- Channel pane unchanged. No Record.

### HTTP (runtime owns it; unchanged from v0.15)

- `POST /v1/agents/{id}/computer/session` → **201** `{ sessionId }`.
- `DELETE /v1/agents/{id}/computer/session` or
  `DELETE …/session/{sessionId}` → **204**.
- While session exists: `POST …/pointer` `{ x, y, type }` and
  `POST …/key` `{ key, type, text? }` in 1280×800 (**200**).
- Channel 409. Agent paused while session exists (POST `/messages` and
  agent computer tools 409). Missing agent 404. No sandbox + POST
  session → 404.

Isolation, report-back, widgets, markdown, MCP, routines Add/Remove,
skill editor, seed DELETE 204, Mac oMLX, no Mac folder picker,
`workspaceTree.ts` (never reintroduce `computerPane.ts`) stand.
Desktop Record / Save as skill stays desktop-only (v0.16).

OpenAPI **0.18.0** (not bumped).

## 26. v0.20 — iOS Record (locked)

**iOS only** (`ios/`). One impl PR. Spec rides this PR. **No second PR.**
Desktop Record/Stop/Save stays **v0.16** — no desktop chrome changes.
Runtime protocol **unchanged**: reuse v0.16
`POST/DELETE /v1/agents/{id}/computer/record` then
`POST /v1/agents/{id}/skills { name }`. Discard is omit that POST.
OpenAPI stays **0.18.0**.

Out of scope: marketplace, pinch-zoom, blank New skill, desktop
changes.

### Product lock (match desktop v0.16)

Record only exists **inside an iOS takeover session** (the v0.19 Open
overlay). Preview `Open` stays as v0.19.

Chrome — 52pt takeover bar:

- 12pt muted `Record` **left of** primary `Done`. **Keyboard stays
  trailing** (v0.19): trailing `Keyboard` then `Record` then `Done`.
- Recording: that control is 12pt `--danger` `Stop` plus a **6pt
  danger dot** (static if Reduce Motion). **`Done` disabled** while
  recording.
- **No Esc** — Stop is the only way out of record (Done is disabled;
  Esc does not Stop).
- Stop opens a `Save as skill` sheet (same family as Edit skill).
  Name 14pt. Primary Save 44pt, **disabled until name**. × / Cancel
  discards — **no SKILL.md**.
- Save writes that agent's skill (v0.18 list, no extra chrome) and
  returns to takeover with `Record` muted again. Optional 1.5s
  `Saved`.
- Keyboard / tap-click / pan-move / Done from v0.19 stay.

### HTTP (runtime owns it; unchanged from v0.16)

- Record only inside an open computer session.
- `POST /v1/agents/{id}/computer/record` → **201** `{ recording: true }`.
  **409** without session / already recording.
- `DELETE /v1/agents/{id}/computer/record` → **204** Stop. Does **not**
  write SKILL.md. Capture stays pending until save or discard.
- `POST /v1/agents/{id}/skills { name }` → **201** Skill `{ id, name }`
  writes SKILL.md (v0.9 load path). **422** empty name / no pending
  capture. Discard is skip this POST.
- Channel id → **409**.

Isolation, report-back, widgets, markdown, MCP, routines Add/Remove,
skill editor, seed DELETE 204, Mac oMLX, no Mac folder picker,
`workspaceTree.ts` (never reintroduce `computerPane.ts`) stand.
Desktop Record / Stop / Save as skill stays as v0.16.

OpenAPI **0.18.0** (not bumped).

## 27. v0.21 — composer `/` skill autocomplete (locked)

Runtime + desktop + iOS. One impl PR. Spec rides this PR. **No second PR.**
OpenAPI stays **0.18.0**. **No new HTTP.** Typeahead list is the existing
`GET /v1/agents/{id}/skills` `{ id, name }`. Channel that GET stays **409**.

Out of scope: marketplace, blank **New** skill, treating **`@skill`** as
a mention. Never reintroduce `computerPane.ts`.

### Product lock (match the existing `@` typeahead family)

**1:1 agent composer only.** Channel `/` is **plain text** — no popup,
no typeahead, no load path.

Trigger: `/` at the **start of a token** (not only at the start of the
whole message). `https://` is not a trigger.

Popup: **240px** (iOS **240pt**) above the composer, `--bg-elevated`
(iOS: same `.bar` family as `@`), **1px / 1pt** border, **8px / 8pt**
radius. Reuse the existing `@` overlay slot and keyboard — do not
invent a second typeahead system.

Rows: desktop **36px**, iOS **44pt**. Name **14px / 14pt**. **No avatar.**
Filter by **name** as they type.

↑↓ + Enter or click/tap inserts **`/name` as plain text** (not a chip,
not an `@`). Esc closes. Sending that message is what loads
**SKILL.md** — **no extra chip chrome in the transcript**.

Empty skill list **or no match**: **no popup, no empty copy**.

### Runtime (no new HTTP)

On ``POST /v1/agents/{id}/messages`` when `{id}` is a **1:1 agent** (not
a channel): scan the user text for `/` at a token start (same trigger as
typeahead). Match `/slug` or `/Name` with that agent’s `load_skills` /
`find_skill` (slug or frontmatter name, case-insensitive). On match,
inject **that** skill’s SKILL.md body into this turn’s prompt — the
named recipe, not only the catalog `skills_preamble`. Unknown `/foo`
stays plain text: **do not 422**, **do not persist extra chrome**.
Channel `snorlax-bot-group` (any `kind=channel`): `/` stays plain text —
no load path. Typeahead list remains existing `GET /v1/agents/{id}/skills`.
OpenAPI stays **0.18.0**.

Isolation, report-back, widgets, markdown, MCP, routines Add/Remove,
skill editor, iOS Record, seed DELETE 204, Mac oMLX, no Mac folder
picker, `workspaceTree.ts` (never reintroduce `computerPane.ts`) stand.

OpenAPI **0.18.0** (not bumped).

## 28. v0.22 — blank New skill (locked)

Runtime + desktop + iOS. One impl PR. Spec rides this PR. **No second PR.
No new HTTP routes.** OpenAPI stays **0.18.0**. Document optional `body`
on existing `POST /v1/agents/{id}/skills`. Mock remains CI default.

Out of scope: marketplace catalog, treating **`@skill`** as a mention,
Slack/GitHub trigger builder, blank New *routine*, `/` autocomplete
changes, Box/takeover/Record chrome. Never reintroduce `computerPane.ts`.

### Product lock

Skills header: 12px muted `Skills` + trailing 12px `Add` (iOS 12pt).
Empty still `No skills yet.` — **Add still shows**. Rows keep `Edit` /
`Remove`.

Sheet: **320px**, title **`New skill`** (not “Add skill”). Name 14px /
14pt. Body 12px/1.45 mono textarea (iOS TextEditor 12pt/1.45),
min-height 200px / 200pt — **source, not rendered preview**. Primary
`Add` 36px desktop / 44pt iOS, **disabled until name AND body**. ×
discards — writes nothing.

iOS matches. `/` typeahead unchanged. Record-to-skill stays
(`POST { name }` with pending capture).

### Runtime (same POST, two bodies)

`POST /v1/agents/{id}/skills` stays one route. Discriminate on `body`
omitted vs present. Do not treat omitted body as blank create. Do not
add a second POST path.

1. Blank New skill: `{ name, body }` (body present, non-empty). Write
   `SNORLAX_DATA_DIR/skills/<slug>/SKILL.md` via `slugify_skill_name`.
   Do **not** require a computer capture. **201** Skill `{ id, name }`
   (listed shape, not SkillBody). Body is the same shape as PATCH: full
   SKILL.md source (frontmatter + recipe) or recipe-only. Empty name or
   empty body → **422**. `kind=channel` → **409**. Missing agent →
   **404**.
2. Record-to-skill (unchanged): `{ name }` with body omitted. Still
   `take_capture` + `write_taught_skill`. 201 `{ id, name }`. Empty name
   or no pending capture → 422 as today.

Unchanged: GET list `{ id, name }`, GET/PATCH/DELETE `/skills/{sid}`,
`/` typeahead load path.

Isolation, report-back, widgets, markdown, MCP, routines Add/Remove,
skill editor, iOS Record, composer `/`, seed DELETE 204, Mac oMLX, no
Mac folder picker, `workspaceTree.ts` (never reintroduce
`computerPane.ts`) stand.

OpenAPI **0.18.0** (not bumped).

## 29. v0.23 — Slack/GitHub inbound listeners (locked)

Runtime + desktop + iOS. One impl PR. Spec rides this PR. **No second
PR. No extra client routes.** OpenAPI stays **0.18.0**.

Out of scope: marketplace catalog, VNC, extra channel types,
Linear/Sentry/other platforms, event-picker UI, Origin listeners,
userAllowlist UI, CI-branch picker. Never reintroduce
`computerPane.ts`.

### Product lock

Cron / webhook / Copy / pause / Remove unchanged. **No Connect CTA in
the pane. No event picker. No marketplace.**

Add-routine sheet segmented control grows **only if** that MCP plugin
is `connected`: `Schedule` / `Webhook` / `Slack` / `GitHub` in **one
12px row**. Unconnected plugins **omit that segment** (do not show a
disabled Slack/GitHub tab).

- Slack: 14px field, placeholder `#eng`, 12px muted `Channel the bot is in.`
- GitHub: placeholder `owner/name`, 12px muted `One repo. No wildcards.`
- Primary `Add` 36px / iOS 44pt, disabled until name + skill + the
  field for that segment (cron / channel / repo). Webhook needs no extra
  field. × discards.
- Saved rows keep the v0.13 labels (`Slack #eng` / `GitHub owner/repo`),
  **no Copy**. Fire is still that agent’s 1:1 as A. Pause skips. iOS
  matches.

### HTTP (same POST `/v1/agents/{id}/routines`)

Cron XOR trigger. Cron `{ name, skill, schedule }` unchanged. Webhook
`{ name, skill, trigger: { type: "webhook" } }` unchanged (201 still
includes `webhookUrl`; Copy stays).

- Slack `{ name, skill, trigger: { type: "slack", channel: "#eng" } }`
  → **201** Routine `{ id, name, skill, enabled, kind: "slack",
  label: "Slack {channel}" }`. **No `webhookUrl`.** Require GET
  `/v1/plugins` shows a Slack plugin `status=connected`; else **422**.
  Empty/missing channel **422**.
- GitHub `{ type: "github", repo: "owner/name" }` → **201**
  `kind: "github"`, `label: "GitHub {repo}"`. Repo must be `owner/name`
  (one repo, no wildcards). Require GitHub plugin connected; else
  **422**. Empty/missing/wildcard repo **422**.
- Channel conversation id on this POST stays **422**. GET/PATCH/DELETE
  channel remain **409**.
- GET still **omits** slack/github rows unless that plugin is
  `connected`. PATCH `{ enabled }` pause/resume and DELETE 204
  unchanged.
- Store the match key (channel or repo) on the row (cron column) and
  the human label in `schedule_label` so GET `label` is `Slack #eng` /
  `GitHub owner/name`.
- `RoutineTrigger`: optional `channel` and `repo`; `label` is
  output-only on GET (clients do not POST label).

Fire: connected Slack/GitHub MCP events into `fire_routine_now` on that
agent’s 1:1 as A LEFT (same as webhook). Slack = messages in that stored
channel. GitHub = `pr-opened` / `pr-pushed` / `pr-merged` on that stored
repo (no event picker stored; ignore other events). Pause
(`enabled=false`) skips. Unknown plugin is **422** on create, not a
silent no-op at POST time.

Isolation, report-back, widgets, markdown, MCP, computer
preview/takeover/record, skill editor, blank New skill, composer `/`,
seed DELETE 204, Mac oMLX, no Mac folder picker, `workspaceTree.ts`
(never reintroduce `computerPane.ts`) stand.

OpenAPI **0.18.0** (not bumped).

## 30. v0.24 — curated plugin catalog (locked)

Runtime + desktop + iOS. One impl PR. Spec rides this PR. **No second
PR.** OpenAPI stays **0.18.0**.

Out of scope: public store, search, registry, VNC, extra channel types,
more than Slack+GitHub this slice, Connect CTA in the catalog besides
trailing Add, paid plugins, uninstall-from-catalog. Never reintroduce
`computerPane.ts`.

### Product lock

Custom Add / Connect / Remove **unchanged**. No search, no registry, no
uninstall-from-catalog. **No extra sheet** on Catalog Add.

Settings Plugins, **below the installed list**: 12px muted `Catalog`.
**Two 44px rows only** — `Slack`, `GitHub` — name 14px, trailing 12px
`Add` (iOS 12pt / 44pt; accent, same family as header Adds). Add POSTs
the baked-in transport **with no sheet**, then the row moves to the
installed list as `needsAuth` with v0.10 `Connect`. **If that plugin is
already installed, omit it from Catalog. If both are installed, hide
the Catalog header too.** Empty installed still `No plugins yet.` —
Catalog still shows underneath when nonempty. iOS Settings match.

### HTTP (runtime owns it)

- `GET /v1/plugins/catalog` (Bearer, runtime-global) → **200** JSON
  array, not wrapped. Shape `{ id, name, transport, command?, args?,
  url? }` — same fields PluginCreate needs so clients POST `/v1/plugins`
  with that body as-is (name + transport + command/args or url). **Omit
  nulls.**
- Curated in runtime code. **Exactly two entries** when neither is
  installed, in this order:
  1. Slack: id `slack`, name `Slack`
  2. GitHub: id `github`, name `GitHub`
- Bake a real stdio (or url) transport each that existing
  `POST /v1/plugins` already accepts (`transport` stdio|url). npx stdio
  is fine. Not a remote registry, not `mcp.json`, no search.
- **Omit** a catalog row when `GET /v1/plugins` already has a
  connected or needsAuth plugin whose id or name matches that kind
  (same needle as `plugin_kind_connected`: `slack` / `github` in
  id+name). If both installed → **200 `[]`**.
- Empty catalog is **200 `[]`** (clients hide the Catalog header). Do
  not 404.
- **No** `POST /catalog`. No uninstall-from-catalog. `DELETE
  /v1/plugins/{id}` unchanged.
- `POST /v1/plugins` unchanged: `{ name, transport, command?, args?,
  url? }` → **201** `{ id, name, status }`. Catalog Add is that POST.
  Then needsAuth Connect is existing `POST /v1/plugins/{id}/auth`
  (v0.10).
- `GET /v1/plugins` still `{ id, name, status }` only — do not add
  catalog fields there.

Isolation, report-back, widgets, markdown, MCP, computer
preview/takeover/record, skill editor, blank New skill, composer `/`,
Slack/GitHub inbound listeners, seed DELETE 204, Mac oMLX, no Mac
folder picker, `workspaceTree.ts` (never reintroduce
`computerPane.ts`) stand.

OpenAPI **0.18.0** (not bumped).

## 31. v0.25 — chat attachments (locked)

Runtime + desktop + iOS. One impl PR. Spec rides this PR. **No second
PR.** OpenAPI stays **0.18.0**. Do not bump. No `/v1/chats/` resource.
Never reintroduce `computerPane.ts`.

Out of scope: agent-sent attachments, video, public/unauthenticated
URLs, a second upload route, VNC, a list-endpoint `{ attachments: [] }`
wrapper. Legacy `MessageCreate.images: [{ mime, data }]` and
`GET /v1/images/{id}` stay (persist off-model).

### Product / Design lock

Composer + user-right only this slice. Agent-sent attachments wait.
Paperclip stays. Desktop also drop onto the composer (accent 1px
highlight). iOS paperclip: Photos or Files. Pending chips wrap above
the bar, 6px gap — never clip. Image pending: **56×56** thumb + 20px
×. File pending: **36px** chip, 13px name, 12px muted size, ×. Send is
on if there’s text **or any chip** (image-only / file-only both send).
Over 10MB: 12px `--danger` `Max 10MB.`, send off. Video drop/pick: same
line, `Video isn’t supported yet.` User-right: images stay **220×160**
in the bubble. Files sit above the text as 36px name chips (8px
radius, 1px border, 13px). Chip opens the Bearer URL. Channel and 1:1
both. `@` and `/` unchanged. iOS chips 44pt hit, same wrap.

### HTTP (runtime owns it)

- `POST /v1/agents/{id}/attachments` (Bearer, multipart field `file`)
  → **201** `{ id, kind, name, url, size }`. `{id}` is the same
  conversation as `POST /v1/agents/{id}/messages` (kind=agent 1:1 or
  kind=channel). Missing conversation **404**.
- `kind` is `image` for `image/*` (not video), else `file`.
- `url` is Bearer `GET /v1/attachments/{id}` on this host. 401 without
  Bearer. 404 unknown. No unauthenticated fetch.
- Over **10MB** → **422** `Max 10MB.`
- `video/*` → **422** `Video isn’t supported yet.`
- Empty file → **422** `Empty file.`
- Message POST adds optional `attachmentIds[]`. Empty `content` is ok
  if that list is non-empty (image-only / file-only send). Unknown or
  foreign ids (not created on this conversation) → **422** (do not 404
  the send). Persist on the **user** message. GET messages include
  `attachments: [{ id, kind, name, url, size }]` on **user-right
  only**; assistant rows are `[]`.
- Runtime includes them **in that turn**: image kinds as OpenAI
  `image_url` input; `text/*` files as filename + extracted text; else
  a short `user attached {name}` note. 1:1 and channel both.
- Isolation: A’s 1:1 attachments never appear in B’s; channel
  attachments stay in that channel thread.

Isolation, report-back, widgets, markdown, MCP, computer
preview/takeover/record, skill editor, blank New skill, composer `/`,
Slack/GitHub inbound listeners, curated plugin catalog, seed DELETE
204, Mac oMLX, no Mac folder picker, `workspaceTree.ts` (never
reintroduce `computerPane.ts`) stand.

OpenAPI **0.18.0** (not bumped).

## 32. v0.26 — agent-sent attachments (locked)

Runtime + desktop + iOS. One impl PR. Spec rides this PR. **No second
PR.** OpenAPI stays **0.18.0**. Do not bump. No new client HTTP
routes. No `/v1/chats/` resource. Never reintroduce `computerPane.ts`.
Keep the v0.25 user-upload section; this slice extends GET + left
chrome + runtime attach.

Out of scope: video, VNC, public store, extra channel types, agent
drag-drop onto bubbles, public/unauthenticated URLs, a second upload
route, agent-sent video. Composer paperclip / drop / Photos / Files /
pending chips / Max 10MB. / Video isn’t supported yet. stay exactly as
v0.25.

### Product / Design lock

`kind=message` from the agent paints the same chrome as user-right —
images **220×160**, files 36px name chips (8px radius, 1px border,
13px) — in the LEFT streak, **above** the markdown, left-aligned,
**6px** gap (iOS **6pt**). iOS chips are 36pt with a 44pt hit.
Channel and 1:1 both. `@` and `/` unchanged. Empty `attachments[]`
on a message: no extra chrome. Do **not** paint on tool lines,
widgets, Connect cards, or the channel timeline handoff card.
Do not add an agent-side picker or drag-drop onto bubbles.

### HTTP (runtime owns it)

- No new client routes. Message GET already has `attachments[]`;
  runtime fills them on agent `kind=message` rows. Chip still opens
  Bearer `GET /v1/attachments/{id}`. Same shape as v0.25:
  `{ id, kind, name, url, size }`.
- GET: drop the v0.25 user-only filter. Any `kind=message` row
  (user-right or agent LEFT) may include attachments. `kind=tool`,
  `widget`, and `connect` stay `attachments: []`.
- Composer and `POST /v1/agents/{id}/attachments` plus
  `MessageCreate.attachmentIds` stay user-only. Do not add an agent
  upload route. Empty `content` + `attachmentIds` still 200; unknown
  id still 422. Legacy `MessageCreate.images: [{ mime, data }]` stay
  off-model. Video still 422.
- After a turn, if the agent used `write_file` or a computer
  screenshot, the runtime creates attachment records (same store as
  user uploads) and puts their ids on that assistant `kind=message`.
  Screenshot → `kind=image`. `write_file` → `kind=file`, or
  `kind=image` if the written path is `image/*`. Skip `video/*` and
  over 10MB. Bind onto the `kind=message`, never the tool-trace row.
- Isolation: attachments ride the message. B’s assistant row is not
  in A’s 1:1, so B’s files are not either. Channel thread: speaking
  agent’s message only.

Isolation, report-back, widgets, markdown, MCP, computer
preview/takeover/record, skill editor, blank New skill, composer `/`,
Slack/GitHub inbound listeners, curated plugin catalog, v0.25
user-upload attachments, seed DELETE 204, Mac oMLX, no Mac folder
picker, `workspaceTree.ts` (never reintroduce `computerPane.ts`)
stand.

OpenAPI **0.18.0** (not bumped).

## 33. v0.27 — video attachments (locked)

Runtime + desktop + iOS. One impl PR. Spec rides this PR. **No second
PR.** OpenAPI stays **0.18.0**. Do not bump. No new HTTP routes. No
`/v1/chats/` resource. Never reintroduce `computerPane.ts`.

Keep the v0.25 / v0.26 sections; this slice extends upload + chrome +
runtime bind for video.

Out of scope: VNC, public store, extra channel types, transcription /
watch-video tool, public/unauthenticated URLs, a second upload route,
in-model watch.

### Product / Design lock

Composer: video drop/pick is allowed. Pending chip for video (not the
v0.25 danger line `Video isn’t supported yet.`). Pending video is a
**56×56** poster (grey `--bg-elevated` + **16px** play if no frame) +
× (iOS **16pt** play). Send stays on if text or any chip including
video. Over 50MB video: 12px `--danger` `Max 50MB.`, send off.
Images/files still 10MB (`Max 10MB.`).

Bubble player **220×160**, **8px** radius, **1px** border (iOS **8pt**
/ **1pt**) on user-right AND agent LEFT, only on `kind=message`. Same
placement as images: above markdown/text, left-aligned on LEFT,
**6px** gap (iOS **6pt**). No autoplay. Poster + **24px** play;
click/tap plays inline with native controls (desktop `<video>`, iOS
AVPlayer). Not on tool lines, widgets, Connect cards, or the timeline
handoff card. Chip/player opens Bearer `GET /v1/attachments/{id}`. iOS
44pt hit. Channel and 1:1 both. `@` and `/` unchanged. Isolation: B’s
files/videos never paint in A’s 1:1. Channel thread: speaking agent
only. iOS Photos includes video. No extra sheet. No transcription, no
in-model watch.

### HTTP (runtime owns it)

- Same `POST /v1/agents/{id}/attachments` (Bearer, multipart field
  `file`) → **201** `{ id, kind, name, url, size }`. Lift the v0.25
  `video/*` 422.
- `Attachment.kind` is `image` | `file` | `video`.
  - `image/*` (not video) → `image`, max 10MB (`Max 10MB.`)
  - `video/*` or `.mp4` / `.mov` / `.m4v` / `.webm` / `.avi` / `.mkv`
    → `video`, max 50MB (`Max 50MB.`)
  - else → `file`, max 10MB (`Max 10MB.`)
- Empty file still **422** `Empty file.`
- `GET /v1/attachments/{id}` Bearer unchanged. 401 without Bearer. 404
  unknown. No unauthenticated fetch.
- `MessageCreate.attachmentIds[]` may include video ids. Empty
  `content` is ok if that list is non-empty. Unknown or foreign ids →
  **422**.
- GET attachments on any `kind=message` (user and agent), including
  `kind=video` rows. `kind=tool` / `widget` / `connect` stay `[]`.
- Runtime MUST NOT send video bytes to the model (no transcription, no
  in-model watch). `apply_to_user_content` skips video bytes; a short
  `user attached {name}` stub is fine. Clients play; the turn just
  includes the attachment.
- After a turn, runtime may bind a sandbox video path (`write_file` of
  a video under 50MB) onto that assistant `kind=message` (same store,
  Bearer GET). Skip over 50MB. Isolation rides the message. Do **not**
  add a watch-video tool.
- Legacy `MessageCreate.images: [{ mime, data }]` stay off-model.

Isolation, report-back, widgets, markdown, MCP, computer
preview/takeover/record, skill editor, blank New skill, composer `/`,
Slack/GitHub inbound listeners, curated plugin catalog, v0.25
user-upload attachments, v0.26 agent-sent attachments, seed DELETE
204, Mac oMLX, no Mac folder picker, `workspaceTree.ts` (never
reintroduce `computerPane.ts`) stand.

OpenAPI **0.18.0** (not bumped).

## 34. v0.28 — watch-video tool (locked)

Runtime. One impl PR. Spec rides this PR. **No second PR.** OpenAPI
stays **0.18.0**. Do not bump. No new client HTTP routes. No
`/v1/chats/` resource. Never reintroduce `computerPane.ts`. Desktop +
iOS idle this slice — no chrome changes, no Watch button, no second
player, no transcode UI, no camera.

Keep the v0.27 video-upload section. This slice adds an opt-in built-in
tool so the agent can describe a `kind=video` attachment as **text**.

Out of scope: VNC, public store, extra channel types, auto-inject every
video into the model turn, live camera, client transcode UI, a new
chrome surface, a new HTTP resource, a Watch button, a second player.

### Product / Design lock

No new chrome. The player stays the v0.27 **220×160** (native controls,
no autoplay) on `kind=message` only. `watch_video` is a **normal 12px
muted tool line** under that agent’s LEFT streak: **`Watched {name}`**
(failure: **`watch_video failed`**). Same `kind=tool` family as
`write_file` / `shell` / `web_fetch`. Not a second player, not on the
user-right, not a widget. Isolation stands: B’s video is not in A’s
1:1; a channel thread can watch only that channel’s attachments.

### HTTP (runtime owns it)

- No new client routes. Tool is on the existing tools list (same
  auto-run built-ins as `write_file` / `shell` / `web_fetch`).
- `watch_video` arguments: `{ attachmentId: string }` required (the id
  from `POST /v1/agents/{id}/attachments` / `message.attachments`).
- Lookup on the **current conversation only** (1:1 agent id or channel
  id).
  - unknown id or other conversation → tool result
    `Error: unknown attachment id` (**do not 422** the user POST)
  - `kind != video` (image/file) → `Error: not a video`
  - success → **plain text** tool result, not JSON. Include the
    filename (first line) and a short description. Cap ~8k chars.
    **NEVER** put raw or base64 video bytes in the tool result or the
    user turn.
- `done_summary`: success `Watched {name}`; failure
  `watch_video failed`.
- v0.27 stands: `apply_to_user_content` still skips video bytes (a
  short `user attached {name}` stub only; the stub may include
  `attachmentId` so the model can call the tool). Do **not** auto-call
  `watch_video` on every video attachment. The agent decides.
- Unknown / non-video / foreign id on **message POST `attachmentIds`**
  is still 422 (v0.25/v0.27). Tool lookup errors are visible follow-ups
  like other failed tools.

### How the runtime describes a video

Local only — do not call a cloud video API.

1. Resolve the attachment file on disk.
2. `ffprobe` for duration / size / resolution when available.
3. `ffmpeg` extracts a few frames (typical: three JPEGs across the
   timeline, ~320px wide).
4. If the local OpenAI-compat backend can take `image_url` (not the
   mock CI backend), caption those frames in a **separate** generate
   call. Frame bytes stay out of the chat-model turn; only the caption
   text is appended to the tool result.
5. If ffprobe, frames, or caption fail, still return **name + size**
   (and duration/resolution when probed). That is **success**, not
   `Error`.

Works offline on the Mac/Linux runtime (ffmpeg/ffprobe optional; mock
backend skips vision). No live camera. No client transcode.

Isolation, report-back, widgets, markdown, MCP, computer
preview/takeover/record, skill editor, blank New skill, composer `/`,
Slack/GitHub inbound listeners, curated plugin catalog, v0.25
user-upload attachments, v0.26 agent-sent attachments, v0.27 video
attachments, seed DELETE 204, Mac oMLX, no Mac folder picker,
`workspaceTree.ts` (never reintroduce `computerPane.ts`) stand.

OpenAPI **0.18.0** (not bumped).



# Apps

Client packages talk to the Spark runtime over `/v1` using `SNORLAX_URL` and
`SNORLAX_TOKEN`. They never read `~/.snorlax-bot/` on the Spark.

- `desktop/` — TypeScript + Tauri
- `ios/` — Swift/SwiftUI companion
